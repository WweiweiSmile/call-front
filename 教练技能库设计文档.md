# 教练技能库（Skills）设计文档

> 版本 v1.0 ｜ 编写日期：2026-09-21 ｜ 设计文档（记录**为什么这么设计**）
> 前置文档：[复盘功能设计文档.md](./复盘功能设计文档.md)、[教练知识库设计文档.md](./教练知识库设计文档.md)
> 关联项目：`call-back` 提示词层（`services/prompt_builder.go`、`services/coach_methodology.go`）
>
> **进度（2026-09-21）**：S1 ✅、牌力分类 ✅、S2 ✅、**S3 ✅**（共 14 篇技能，
> 相对牌力已接入路由；六类手牌的实测数据见 §9 的 S2/S3 两节）。
> §11 是拍板记录，§12 是牌力分类设计。
>
> **追问路径定稿**（2026-09-21）：保持精简版常量，不做筛选也不全给。见 §11.1。
>
> **真机验证已完成**（2026-09-21）：三条全部通过，详见 §9.1。
> **成败判据那条通过了** —— 模型不会因为专项技能没展开就推卸责任。
> 但实测也暴露了一个没那么好看的数字：**展开多篇的手牌只省 4.9% token**，见 §9.1。

---

## 1. 问题：现在的注入方式是「全量披露」

### 1.1 现状

`services/coach_methodology.go` 把 Phil Gordon《小绿皮书》压成一个 Go 常量 `coachMethodology`，
文件注释自述**约 6 千字**。它的注入位置是**无条件、全量**的：

| 调用点 | 位置 | 注入内容 |
|---|---|---|
| 手牌分析 | `prompt_builder.go:163-166`（在 `BuildSystemPrompt` 内） | `coachMethodology` 全文 |
| 追问对话 | `prompt_builder.go:411`（在 `BuildChatSystemPrompt` 末尾） | `coachMethodologyBrief`（精简版） |

也就是说：**无论这手牌是「翻前无人入池就结束」还是「河牌 200bb 的诈唬被抓」，模型每次都要先读完这 6 千字。**

### 1.2 三个代价

**代价一 · 注意力稀释（比成本重要得多）**

6 千字方法论里，与某一手牌真正相关的通常只有其中 200~400 字。剩下的内容不是「无害的背景」——
长上下文里的关键信息会被平均化。现有提示词已经在用很硬的措辞对抗这件事：

- `prompt_builder.go:161` —「学生打出被动线时，对照方法论里『建议过牌必须命中的理由』，检查他的过牌能不能命中——命中不了就直接点名」
- `coachMethodology` §五 —「**建议过牌必须命中以下之一**（否则就是错误建议）」

这些句子之所以必须写得这么强硬，本质是在跟「6 千字里的那一条被淹没了」作斗争。

**代价二 · 成本固定**

每一手牌、每一次追问都付这份钱，与手牌内容无关。
（具体增量不给拍脑袋的百分比，见 §8.3 —— 以 `review_analyses.tokens_in` 实测为准，
这是 `教练知识库设计文档.md` §9.1 已经立下的规矩。）

**代价三 · 知识体量有天花板（最致命）**

你的原话是「德州扑克是一个庞大的课程」。而现在想加内容，唯一办法是往这一个常量里继续塞：

- 多人池 3bet 底池的翻后策略
- 短筹码 / ICM
- 深筹码 200bb 单挑
- 现代范围平衡与求解器理论（现在只能写在 §十二 里说「超出本方法论范围」）

塞到 15000 字时，代价一和代价二会同时恶化**得比线性更糟**。现在这套结构，能承载的知识量是有上限的，
而扑克课程远大于这个上限。**这是本设计的真正动机 —— 不是省钱，是解开天花板。**

---

## 2. 什么是 skills 与渐进性披露

### 2.1 一句话定义

> **渐进性披露（Progressive Disclosure）**：把一个庞大的知识体组织成
> **索引层 → 正文层 → 引用层**三层，让读者（模型）先用极小的成本判断「该看哪一篇」，
> 再看那一篇的正文；正文里提到的细节，再按需展开。

关键在于**分层加载**：不是「少给知识」，而是「按需给知识」。

### 2.2 对照 Claude Code 的 Agent Skill

你天天在用的 Claude Code 就是这套机制的典型实现，拿来当参照最直观：

| 层 | Claude Code 里是什么 | 何时进入上下文 | 体量 |
|---|---|---|---|
| 第一层 | `SKILL.md` frontmatter 的 `name` + `description` | **永远在** | 几十 token |
| 第二层 | `SKILL.md` 正文 | 模型判断「这活儿用得上」才读 | 几千字 |
| 第三层 | `references/*.md` | 正文里指到了才读 | 可以任意大 |

你之所以能装几十个 skill 而不撑爆上下文，全靠第一层足够便宜。
**Skill 的价值不在于「打包知识」，而在于「让知识可以无限增长而首屏成本恒定」。**

### 2.3 映射到你的后端提示词层

| 层 | 你的场景里是什么 | 何时注入 |
|---|---|---|
| 第一层 · 目录 | 技能目录：每篇一行 `code \| 什么时候用 \| 一句话核心结论` | **永远注入** |
| 第二层 · 正文 | 命中的技能正文 | 服务端按手牌场景**匹配后**注入 |
| 第三层 · 引用 | （见 §3.2，v1 做不到） | — |

---

## 3. 本场景的适配性与边界

### 3.1 为什么这个场景特别适合（核心洞察）

**因为一手牌在进入分析之前，就已经结构化地告诉了你「该披露哪一篇」。**

一般场景做渐进性披露，最难的是路由 —— 所以大家才要上 embedding、向量检索、相似度打分。
你的场景不是这样。看 `BuildAnalysisPrompt(hand, tags, memory)` 拿到的 `hand`，它在调用模型之前就已知：

| 路由信号 | 来源字段 | 例子 |
|---|---|---|
| 打到哪条街 | `hand.Streets[].Street` | preflop / flop / turn / river |
| hero 位置 | `hand.HeroPosition` | BTN |
| 几人桌 | `hand.TableSize` | 6 |
| 单挑还是多人 | `hand.PotType` | hu / multi |
| 筹码深度 | `hand.HeroStackBB`、`VillainInfo.StackBB` | 100bb / 25bb |
| 牌面结构 | `hand.Board`（需解析，见 §5.3） | A♠7♠2♦ = A高 + 两张同花 |
| 底池体量 | `utils.ComputeStreetPots` | 小池 / 大池 |
| 行动序列强度 | `hand.Streets[].Actions`（需数，见 §5.4） | 翻前 4 个动作 = 3bet 底池 |

**「6 人桌、BTN、翻前 3bet 底池、100bb、翻牌 A 高两张同花、hero 有位置」——
这句话拿去匹配技能，准确率远高于任何向量检索。**
因为这些维度全是**枚举或可精确计算的值**，没有模糊地带。

这与 `教练知识库设计文档.md` §5.3「为什么不上 L2 向量检索」是同一个判断，两边互相印证。

### 3.2 边界：做不到 Claude Code 那种「模型自主取阅」（必须诚实说明）

**Claude Code 的 skill 是模型自己决定去读文件** —— 它有工具、有 agentic loop，可以读一半、
发现不够、再读一个。**你的分析链路没有这个过程**：`ai_client.go` 的 `CompleteJSON` 是**一次性调用**，
模型没有 tool use 循环，它不能主动说「我要看深筹码那一篇」。

所以：

> **在你的场景里，渐进性披露 = 服务端根据手牌场景「预判式筛选」，而不是模型自主取阅。**

这个差别决定了设计重心：**匹配函数的准确率就是整套机制的生命线**（§5）。
将来如果引入 tool use（让模型能反问「这手牌有没有更深的资料」），才谈得上第三层。

---

## 4. 技能切分设计

### 4.1 切分原则：按「决策场景」切，不按「知识点」切

模型需要的不是「关于持续下注的知识」，而是「**这个局面我该怎么打**」。
所以一篇技能的边界，应当是一个**玩家在牌桌上会真实遇到的决策节点**。

反例：「下注尺度理论」——它不是决策节点，是跨场景的工具，拆出来会导致每篇都要引用它。

### 4.2 常驻技能（不参与匹配，永远注入）

有两块内容**必须常驻**，因为它们不是「某个场景的知识」，而是所有场景的共同前提：

| code | 内容 | 来源 | 为什么常驻 |
|---|---|---|---|
| `core-stance` | 底层立场 + 下注的理由清单 + 行动建议决策流程 + 已知边界 | §一 + §二 + §五 + §十二 | 「先问能不能加注」「说不出理由的下注就是漏损」「决策质量不是输赢」是所有技能共用的口径。缺了它，各篇技能会各说各话 |
| `odds-table` | 赔率与概率速查（BEP 表、四二法则、常用概率） | §十 | 赔率是跨场景的**计算工具**，几乎每篇都会用到。约 300 字，常驻的性价比最高 |

`core-stance` 常驻还有第二个作用：**它是「匹配全部落空」时的保底**（§6.3）。

### 4.3 匹配型技能（第一版 12 篇）

| # | code | 何时用 | 来源 | 估算字数 |
|---|---|---|---|---|
| 1 | `preflop-open` | hero 翻前首个入池决策 | §六（开池尺度部分） | ~350 |
| 2 | `preflop-vs-open` | 翻前面对别人开池 | §六（跟注/再加注部分） | ~400 |
| 3 | `preflop-3bet-pot` | 翻前形成 3bet 及以上的底池 | §六（套池、KK/QQ） | ~350 |
| 4 | `postflop-cbet` | hero 是翻前加注者、翻后先行动 | §七（持续下注频率表、四因素法、对子面先下注） | ~700 |
| 5 | `postflop-vs-cbet` | hero 面对别人的翻牌下注 | §七（赔率对上 outs） | ~350 |
| 6 | `postflop-multiway` | 翻后 3 人及以上 | §七（多人池铁律） | ~200 |
| 7 | `postflop-draw` | hero 持听牌 / 半诈唬决策 | §七（优质抽牌、封锁下注）+ §八（半诈牌清单、隐含赔率） | ~500 |
| 8 | `postflop-made-hand` | hero 翻后持有成牌，**按相对牌力触发**（§12） | §七（两对/三条/明三条/顺/花/葫芦） | ~800 |
| 9 | `postflop-turn` | 打到转牌 | §八（「转牌不是耍花招的时候」、吓人牌、控池） | ~450 |
| 10 | `river-value` | 河牌 hero 持强牌、考虑价值下注 | §九（钟形曲线、让价值下注不像价值下注） | ~250 |
| 11 | `river-medium` | 河牌 hero 中等牌力 / 抓诈唬 | §九（无位置中等牌下注是最糟错误、河牌翻转规律） | ~350 |
| 12 | `opponent-profiling` | 有对手信息（≥1 个有名字或有位置的对手） | §三（五格分类 + 硬规则 + 六种剥削模式） | ~700 |
| 13 | `range-reading` | 有对手信息、且打到翻牌及以后 | §四（四条线索、范围合成顺序、禁止事项） | ~600 |
| 14 | `mindset` | `heroThought` 命中情绪关键词 | §十一 | ~300 |

> **字数全是估算**，且必须在 S2 用 `tokens_in` 实测校准（§9）。
> §六/§七 之所以拆成多篇，是因为它们是全文最长的两节（合计约占一半篇幅），
> 且内部本来就是按「开池 / 面对开池 / 3bet」「持续下注 / 面对下注 / 多人池 / 成牌」分段的。

**这个映射表的意义：证明 6 千字能被拆干净、不丢内容。**

> **已实施的范围（S3 后共 14 篇）**
>
> | 类别 | 技能 | 触发依据 |
> |---|---|---|
> | 常驻 | `core-stance`、`odds-table` | —— |
> | 对手 | `opponent-read` | 有对手信息 |
> | 翻前 | `preflop-open`、`preflop-vs-open`、`preflop-3bet-pot` | `PreflopShapes` |
> | 翻牌 | `postflop-cbet` | `CbetSpot`（翻前加注者且翻牌没被下注） |
> | | `postflop-made-hand` | `MinMade`（两对及以上） |
> | | `postflop-draw` | `RequireDraw` |
> | | `postflop-multiway` | 多人池 |
> | 转牌 | `postflop-turn` | 街道 |
> | 河牌 | `river-value` | 街道 + 相对牌力强以上 |
> | | `river-medium` | 街道 + 相对牌力弱~中等 |
> | 心态 | `mindset` | 情绪关键词 |
>
> **与上表的两处差异，都是实现时发现设计站不住：**
>
> 1. **没有 `postflop-vs-cbet`。** 设计表说它的来源是「§七 赔率对上 outs」，
>    但 §七 里根本没有「面对别人下注」这一节 —— 赔率内容全在 §十，
>    而 §十 已经是常驻的 `odds-table`。**这一篇没有源内容可放**，建了就是空壳。
> 2. **牌面结构没有成为独立的触发维度。** 设计 §5.3 原计划加 `BoardTraits`，
>    但实现下来没有任何一篇技能需要按牌面结构路由 —— 因为牌面结构已经通过
>    **相对牌力档位**进入了路由（§12）：同一个顺子在干燥面是「强」，
>    在「同花面 + 公对」上是「中等」，两者自然路由到 `river-value` / `river-medium`。
>    这恰好印证了 §12.6「`AnalyzeBoard` 只能有一份实现」的判断。
>
> 另外 `opponent-profiling` + `range-reading` 合并成了 `opponent-read`（§三+§四）。

### 4.4 目录条目的形态

目录不是纯索引 —— **每篇带一句话核心结论**，所以「未命中」的代价是「细节不够」而不是「完全不知道」：

```
## 教练技能目录
以下是本次点评可用的技能。打了 ★ 的是**本次已展开全文**的，请主要依据它们；
未展开的只有一句结论，不要凭它展开细节（见硬性约束 15）。

★ core-stance     — 底层立场与行动优先级（哪条都适用）
★ odds-table      — 赔率、outs 与常用概率速查
★ postflop-cbet   — 你是翻前加注者，翻后先行动｜结论：按单挑情形取 65%/85%/50% 频率，尺度用四因素法
  postflop-draw   — 你在听牌或考虑半诈唬｜结论：主动下注有两条赢路，但转牌面对 1/2 池以上的跟注是错的
  river-medium    — 河牌中等牌力｜结论：无位置下注是最糟错误之一，过牌引诱诈唬
  ...
```

`★` 标记让模型清楚知道**哪些是全文、哪些只是摘要**，这是防止它拿一句话硬编正文的关键（§8.1 第 15 条）。

---

## 5. 触发条件与匹配算法

### 5.1 触发器结构

技能自带的触发条件是**代码里写死的，用户不可见也不可编辑**（这一点与知识库相反，见 §7）。
用 Go 结构体而非 JSON，因为它是编译期常量，写错应当在编译期就暴露：

**已实现**（`services/skills.go`）的字段：

```go
// SkillTrigger 技能的路由条件。各非零维度取交集；全零 = 不命中。
type SkillTrigger struct {
    Streets    []string // models.StreetPreflop 等
    Positions  []string // 匹配 hand.HeroPosition
    PotTypes   []string // hu / multi
    TableSizes []int    // 2~9

    // MinActions 某条街的最少行动条数，用来识别「3bet 底池」这类
    // 枚举字段表达不了的局面（见 5.4）
    MinActions map[string]int

    // CbetSpot 要求 hero 处在「该不该持续下注」的决策点
    CbetSpot bool
    // MinMade 要求 hero 的**绝对**成牌等级至少到这一档（零值 MadeNone = 不要求）
    MinMade utils.MadeHand
    // 相对牌力闭区间。要有独立开关：Tier 的零值是 TierAir，
    // 拿零值表示「未设置」就等于「只匹配空气」
    RequireStrength bool
    MinStrength     utils.Tier
    MaxStrength     utils.Tier
    // RequireDraw 要求 hero 有听牌
    RequireDraw bool

    Keywords []string // 扫 hand.HeroThought 与 hand.Title，任一命中即可
    Always   bool     // 常驻，跳过全部匹配，且不占篇数上限
}
```

**牌面结构与筹码深度最终没有成为触发维度**：
- 牌面结构不需要 —— 它已经通过相对牌力档位进入路由（见 §4.3 的两处差异）
- 筹码深度仍未做 —— 它要先定「有效筹码」的口径（`min(hero, 各对手)` 还是只看 hero）

### 5.2 匹配函数（纯函数）

抽成纯函数、不依赖数据库，便于单测 —— 与 `AggregateInsights`（`review_memory.go:189`）
和知识库的 `SelectKnowledge`（`教练知识库设计文档.md` §5.4）同款处理：

```go
// SelectSkills 按手牌场景筛选本次要展开全文的技能。
// 抽成纯函数是因为这是整套机制最容易出错的一环（通配、交集、排序、截断），
// 必须能脱离数据库与模型单独测。
func SelectSkills(all []Skill, hand *models.ReviewHand) []Skill
```

匹配全部通过才算命中（各非零维度取交集），与知识库 `matchScope` 语义一致。

### 5.3 牌面结构：`utils.AnalyzeBoard` —— **已实现**（S3 提前完成）

> **修正**：设计初稿把它叫 `ClassifyBoard` 放在 `services` 里，实现时改成了
> `utils.AnalyzeBoard`。原因是它有两个消费者 —— 技能触发器与相对牌力的降级判断
> （§12）—— 必须只有一份实现。放进 `utils` 与 `NormalizeCards` / `ComputeHandHash`
> 同级，两边都能用。详见 §12.6。

`hand.Board` 是 `"AsKh7s"` 这样的字符串。牌面结构是路由的关键信号，必须先解析出来：

```go
// BoardTexture 公共牌的结构特征（utils/hand_strength.go，已实现）
type BoardTexture struct {
    Paired       bool // 有对子（664 / KK6）
    TripsOnBoard bool // 三条面（KKK）
    ThreeFlush   bool // 三张同花
    FourFlush    bool // 四张同花（转牌/河牌）
    Connected    bool // 顺面（三张跨度不超过 4）
    AceHigh      bool // 含 A
    HighRank     int  // 最大点数
    // 可扩展：StraightPossible / FlushPossible（按街递增）
}
```

**为什么这是最值得单测的**：它是 O(5 张牌) 的纯逻辑，输入输出完全确定，
而错了会导致整篇技能被错判（例如「对子面先下注」那条永远不会被注入）。
建议直接表驱动单测覆盖 30+ 个真实牌面。

### 5.4 翻前局面形态：`PreflopShapes` —— **设计初稿的 `MinActions` 是错的**

> **修正**：设计初稿提出用 `MinActions`（某条街的最少行动条数）来识别「3bet 底池」，
> 例如「翻前 ≥3 个行动且有 raise」。**实现时发现这个判据站不住**：下面两手牌
> 翻前都是 3 个行动，决策场景却完全不同 ——
>
> | 行动序列 | 动作数 | 真实场景 | `MinActions>=3` 的误判 |
> |---|---|---|---|
> | BTN 加注 / SB 弃 / BB 弃 | 3 | hero 开池，无人应战 | 若阈值取 3 会误判成 3bet 底池 |
> | BTN 加注 / SB 加注 / BB 弃 | 3 | hero 面对 3bet | — |
>
> 根因是：**「3bet 底池」不是行动条数的函数，而是「hero 行动之前有几次加注」的函数。**

改用 `PreflopShapes`：逐个看 hero 的翻前行动，数它**之前**有几次加注。

```go
const (
    ShapeUnopened    = "unopened"     // hero 行动前无人加注 —— 开池决策
    ShapeSingleRaise = "single-raise" // hero 行动前恰好一次加注 —— 面对开池
    ShapeThreeBet    = "three-bet"    // hero 行动前已有再加注 —— 3bet 底池
)

// 返回 hero 面对过的形态**集合**（不是单个值）
func PreflopShapes(hand *models.ReviewHand) []string
```

**为什么是集合而不是单个值**：hero 的每一次翻前行动都是一个决策点。
「hero 开池后被 3bet 再弃牌」有两个决策点（开池、面对 3bet），
两篇技能都该展开 —— 返回单个值会漏掉其中一个。

`allin` 计入加注：翻前的全下几乎总是再加注，不计的话短筹码的 3bet 底池会被判成无人加注。

### 5.5 排序与截断

命中多篇时按以下顺序取前 N 篇（N 见 §6.2）：

1. `priority` 降序（技能自带，人工指定）
2. **「命中维度数」降序** —— 触发器填得越具体，说明越针对当前场景
3. `code` 字典序（保证结果确定，不随 map 遍历顺序抖动）

第 3 条是必须的：Go 的 map 遍历顺序随机，不钉死的话**同一个手牌两次分析的提示词会不一样**，
`input_snapshot` 就失去了可复现性，调试时会被误导。

---

## 6. 三层结构与预算

### 6.1 注入位置

改造后 `BuildSystemPrompt` 的结构（现有结构见 `prompt_builder.go:112-180`）：

```
[System]
  ├ 角色设定                    ← 不变
  ├ 输出格式（JSON Schema）      ← 不变
  ├ 硬性约束 1-12               ← 不变
  ├ 【新增】硬性约束 13-16       ← §8.1
  ├ 分析视角                    ← 不变
  ├ 教练技能目录（第一层）        ← 【新增】永远注入
  ├ 本次展开的技能正文（第二层）  ← 【新增】匹配后注入
  │   ├ core-stance   （常驻）
  │   ├ odds-table    （常驻）
  │   └ <命中的技能>...
  ├ 安全声明                    ← 不变
  └ 标签表                      ← 不变
```

**为什么技能在 `[System]` 内，而 Memory / Knowledge 在 user prompt 里？**
因为两者性质不同：技能是**通用打法规则**（不随用户变化，是"教练的知识"），
Memory 与 Knowledge 是**这位学员的具体情况**（随用户变化，是"教练对你的了解"）。
规则放 system、数据放 user，这个分层是干净的，也顺带让 system 部分能被提示词缓存命中。

### 6.2 预算

| 参数 | 建议值 | 说明 |
|---|---|---|
| `SkillCatalogMaxRunes` | 900 字 | 目录层硬顶（约 20 篇 × 45 字） |
| `SkillBodyMaxRunes` | 4000 字 | 本次展开正文的合计上限 |
| `SkillMaxExpanded` | 6 篇 | 展开篇数上限（不含常驻） |

对照现状：现在恒定注入约 6000 字。改造后典型情况是
**常驻 ~800 字 + 目录 ~600 字 + 命中 3~5 篇 ~2000 字 ≈ 3400 字**，
比现在少，但**模型面对的相关正文从 6000 字降到 2000 字左右** —— 注意力集中度的改善比字数缩减更有价值。
而目录层可以继续增长到 30~40 篇而不显著变贵，**天花板解开了**。

超预算时按 §5.5 的顺序丢弃，并且**必须留痕**：被丢弃的技能写进后端日志。
`input_snapshot`（`review_analysis_service.go:236`）存了完整提示词，事后能直接看到那次展开了哪几篇。

### 6.3 匹配全部落空怎么办

**刻意不设「全量兜底」。** 理由：带兜底等于留了一个永远会退化的后门，
匹配函数的 bug 永远不会被发现。

取而代之的三层保障：

1. `core-stance` + `odds-table` 常驻 —— 立场、决策流程、赔率口径永远在线
2. 目录层始终注入，且每篇带一句话结论 —— 模型不至于完全无据可依
3. 落空时在提示词里显式说明（见 §8.1 第 16 条），让模型知道「细节未展开」而不是「这事没规矩」

---

## 7. 与知识库（K1~K4）的关系

两者机制高度相似（scope 匹配 + 预算截断 + 结构化块注入），**必须说清楚否则会重复建设**：

| | **技能库**（本篇） | **知识库**（K1~K4） |
|---|---|---|
| 谁写的 | **你**（开发者） | **用户** |
| 存在哪 | Go 源码常量 / `go:embed` 的 `.md` | MySQL `review_knowledge` 表 |
| 能不能改 | 改了要发版 + 升 `PromptVersion` | 用户随时改 |
| 内容 | 扑克通用打法（小绿皮书的展开） | 用户的个人主张与背景 |
| 提示词层 | 在 `[System]` 内 | 在 Memory 之后（user prompt） |
| 触发器 | 代码里的 `SkillTrigger`，用户不可见 | 用户填的 `scope` |
| 用户能顶撞吗 | 不适用（是权威来源） | **能**，见知识库 §4.3 |

**不冲突，且共享骨架**：建议把匹配与截断抽成通用工具（`matchScope` / `sortAndTruncate`），
两边复用。**这是省工作量的关键 —— 不要写两套。**

三者的提示词顺序天然正确：
技能（通用打法）→ Memory（你实际怎么打）→ Knowledge（你以为你怎么打）。

---

## 8. 输出契约、版本与指纹

### 8.1 必须新增的硬性约束

现有硬性约束到第 12 条（`prompt_builder.go:140-152`）。技能化之后必须补：

```
13. 系统提示里的「教练技能」是你本次点评的依据。标注 ★ 的技能已展开全文；
    未标注的只有一句核心结论。请主要依据已展开的技能下结论。
14. **未被展开的技能不等于不存在**。不要因为没看到某方面的内容就说「超出我的范围」
    或「这需要更专业的分析」——按你已有的知识与目录里的结论正常作答。
15. **不要凭目录里的一句话结论展开细节**。目录只用来让你知道「有哪些方面可考虑」；
    展开全文的技能才有足够的细则支撑具体尺度与频率。
16. 若本次除常驻技能外没有任何专项技能被展开（目录里只有 core-stance 与 odds-table 带 ★），
    说明这手牌缺少可细化的场景，按核心立场与赔率速查作答即可，不要硬套某一套细则。
```

第 14 条尤其重要：**它是防止渐进性披露退化成「模型推卸责任」的关键**。
（这是本设计里最需要实测验证的一条，见 §9.1。）

> **第 16 条按实现修正过**：初稿写的是「若本次没有任何技能被展开」，
> 但 `core-stance` 与 `odds-table` 是常驻，**目录里不可能全是未标注项**，
> 那条永远不会触发。改成按「除常驻外没有专项技能被展开」来判断。

### 8.2 版本与指纹

`models.CurrentPromptVersion` 现在是 `"v2.0"`（`models/review_analysis.go:26`）。

**技能库一改，旧分析结论就不再对应当前依据。** 处理方式：

- **建议**：技能集变化时**必须升 `CurrentPromptVersion`**（如 `v3.0-skills`）。
  改技能是发版行为，本来就该让历史分析可被识别为「依据已更新」。
- **不建议**并入 `ContentHash`。`ContentHash`（`utils.ComputeHandHash`）的语义是
  「手牌内容变没变」，把技能指纹混进去会让语义漂移，且会给 `VillainInfo` 加字段那条
  硬约束（`omitempty`）再添一个坑。

**但这会导致一个问题**：`Stale` 现在只看 `ContentHash`（`controllers/review_controller.go:244`），
**不看 `PromptVersion`**。所以升版本后，前端不会提示用户「结论依据已更新」。
是否要给 `Stale` 加一条 `PromptVersion != CurrentPromptVersion` 的判断，列入 §11 待确认。

**无论怎么定，都不自动重跑** —— 一手牌约 7~8 分钟，重跑全是钱（与知识库 §7.1 方案 B 被否掉的理由相同）。

### 8.3 成本必须实测，不给拍脑袋的数字

沿用 `教练知识库设计文档.md` §9.1 的规矩：
`review_analyses.tokens_in` 已经记了每次分析的输入 token（`review_analysis_service.go:186`），
**S2 上线前后各跑同一批手牌，对比这个字段**。§4.3 的字数全是估算，以实测为准。

---

## 9. 里程碑拆解

按你的习惯，每个里程碑停下来交付并给验收清单，不顺手往下做。

### S1 · 骨架与目录层（**零行为变化的等价重构**）—— ✅ 已完成（2026-09-21）

把 `coachMethodology` **原封不动**装进一篇技能 `core-methodology`，触发器 `Always: true`，
走完整的「筛选 → 拼装 → 注入」链路，**不拆任何内容、不启用任何匹配**。

**这一步的价值不是新功能，是建立一个「重构没改变现状」的基线。**
它是唯一能用 diff 证明「地基换好了」的做法。目录层也可以在这一步就建好（只有一篇）。

验收：
1. 挑 3 手不同类型的手牌，改造前后 `input_snapshot` **逐字 diff 为空**（或仅有结构标记的差异，逐条确认）
2. `SelectSkills` 的单测：`Always` 命中、非 `Always` 全零触发器不命中
3. 跑通一次真实分析，确认 `tokens_in` 与改造前同手牌基本一致

### S2 · 拆翻前 + 启用匹配 —— ✅ 已完成（2026-09-21）

实际拆了 **8 篇**（比原计划多，原因见 §4.3 的实施状态说明）：
`core-stance` / `odds-table` 常驻；`preflop-open` / `preflop-vs-open` / `preflop-3bet-pot`
由 `PreflopShapes` 触发；`opponent-read` 由 `MinVillains` 触发；
`postflop-numbers` 由街道触发；`mindset` 由情绪关键词触发。

拆分过程是**脚本按行切片**而不是手抄（正文含 Markdown 表格与中文引号，手抄必丢），
并做了机械校验：原文 165 个非空行逐行确认落到新文件，§六 的 7 条按预期分布到三篇。

#### 实测：技能层字数的变化

以改造前恒定的 6236 字为基准（`coachMethodology` 全文）：

| 手牌 | 展开篇数 | 技能层字数 | 对比旧版 |
|---|---|---|---|
| 翻前开池就结束·无对手信息 | 3 | 3036 | **−3200** |
| 翻前面对开池后弃牌·有对手 | 4 | 4421 | **−1815** |
| 打到河牌·有对手 | 6 | 6597 | **+361** |
| 打到河牌·有对手·带情绪 | 7 | 6866 | **+630** |

**必须如实说明：打到河牌的手牌，S2 之后提示词反而变大了一点。**
原因是目录层（874 字）在每一步都要付，而 `postflop-numbers` 这一篇仍有 1982 字没拆。
**S2 的真实收益是「简单手牌大幅变小」，不是「全都变小」。**
全手牌变小要等 S3 把翻后那 1982 字拆开 —— 这是继续做 S3 最直接的理由。

#### 验收

| 项 | 结果 |
|---|---|
| §六 内容无遗漏无重复 | ✅ 脚本逐行核对，7 条按预期分布 |
| 匹配在真实手牌上的表现 | ✅ 4 类场景实测，展开集符合预期 |
| `PreflopShapes` 取代 `MinActions` | ✅ 8 个表驱动用例（含老数据 `actor=hero`、全下算加注） |
| 目录层 | ✅ ★ 标记、出处、「结论要能指回规则」三项都有断言 |
| 每篇正文原样出现在提示词里 | ✅ 逐篇断言 |
| `tokens_in` 实测 | ⏳ **未做** —— 需要真实调模型，见 §9.1 |

### S3 · 拆翻后 + 接入牌力路由 —— ✅ 已完成（2026-09-21）

把 `postflop-numbers`（§七+§八+§九，1982 字）按决策场景拆成 7 篇，
并把 §12 的牌力分类接进路由。触发维度新增：`CbetSpot`、`MinMade`、
相对牌力闭区间、`RequireDraw`（**不含牌面结构**，理由见 §4.3）。

#### 实测：六类手牌（与 S2 同一批，可直接对比）

| 手牌 | S2 | **S3** | 说明 |
|---|---|---|---|
| 翻前开池就结束·无对手信息 | 3036 (−3200) | 3340 (−2896) | 目录变长抵消了一部分收益 |
| 翻前面对开池后弃牌·有对手 | 4421 (−1815) | 4688 (−1548) | 同上 |
| 打到河牌·有对手 | 6597 (**+361**) | 5263 (**−973**) | ✅ 由增转减 |
| 打到河牌·有对手·带情绪 | 6866 (**+630**) | 5453 (**−783**) | ✅ 由增转减 |
| 河牌持三条·干燥面 | — | 5904 (−332) | |
| 河牌持两对·连通面 | — | 5922 (−314) | 走 `river-medium`，见下 |

**S3 达成了它的核心目标：河牌手牌从「比旧版大」变成「比旧版小」。**
代价是简单手牌的收益被目录层吃掉了一部分（目录随技能数增长，S2 的 874 字 → S3 的 1125 字）。

#### 顺带修掉的一个真问题：目录层的冗余

S3 加到 14 篇后，目录层一度涨到 **1367 字，超了 §6.2 定的 900 字硬顶 52%**。

根因是**★ 已展开技能的「一句话结论」是纯冗余** —— 它的全文就在下面紧跟着。
设计 §4.4 说「未展开的代价是细节不够」，那句话只对**未展开**的技能有意义。
改成：★ 的只列代号与标题，未展开的才带「什么时候用｜结论」。
再去掉目录头部与硬性约束 13/15 重复的两句使用说明，目录降到 **829~1125 字**
（展开越多目录越短）。

> **目录是这套机制里唯一随技能数线性增长的部分。** 14 篇时最坏 1125 字，
> 仍在可接受范围，但**再加到 30 篇以上时它会成为主要开销**，
> 那时需要考虑按场景裁剪目录条目，或把目录也做成两层。

#### 关于 `river-medium` 那条测试

同一手两对，牌面不同 → 展开的技能不同：

| 牌面 | 相对牌力 | 展开 |
|---|---|---|
| 干燥面（三条，无同花无公对） | 强 | `river-value` |
| 连通面（两对，98732 这种） | 中等（连通面降一档） | `river-medium` |

**这就是相对牌力影响路由的直接证据**，已固定成单测
（`TestSelectSkillsStrengthRoutingDependsOnBoard`）。

#### 验收

| 项 | 结果 |
|---|---|
| §七/§八/§九 内容无遗漏无重复 | ✅ 脚本逐行核对，54 行全部落地 |
| 相对牌力影响路由 | ✅ 单测固定（同一个顺子 → 不同技能） |
| 牌力算不出来时不误命中 | ✅ 手牌/牌面缺失时该维不命中 |
| `CbetSpot` 的边界 | ✅ 5 个用例，含「翻牌面对下注不算持续下注」 |
| 篇数上限裁掉的是最不相关的 | ✅ 单测断言翻前细则先被裁 |
| ≥14 篇时的展开集合理 | ✅ 实测 3~8 篇 |
| `tokens_in` 实测 | ⏳ **未做**，见 §9.1 |

### S4 · 补齐与打磨

`opponent-profiling` / `range-reading` / `mindset` + 技能命中率日志 + 目录层截断。

验收：
1. 日志能看出「哪些技能从没被命中过」与「哪些总是被截断」——
   与知识库 §6.4 命中率反馈同理，只不过读者是你自己，用来指导技能库迭代
2. 累计 30+ 手真实手牌跑过一遍，统计各技能命中分布，据此调整触发器

---

### 9.1 真机验证结果（2026-09-21 ✅ 已跑）

跑法（默认跳过，会真实调模型、每次两三分钟）：

```bash
cd ~/codes/call-back && set -a && . ./.env && set +a
VERIFY_REAL_MODEL=1 VERIFY_USER_ID=11 go test ./services/ -run TestVerify -v -timeout 30m
```

用 `deepseek-v4-pro` 实测，三条验证点：

| # | 验什么 | 结果 |
|---|---|---|
| 1 | **模型会不会在专项技能未展开时推卸责任**（成败判据） | ✅ **通过** —— 见下 |
| 2 | 会不会拿目录里的一句话结论硬编细则 | ✅ 通过 |
| 3 | `tokens_in` 的真实变化 | ⚠️ 只省 4.9%，见下 |

#### 验证点 1：没有推卸责任（这是本方案的成败判据）

用一手「河牌持 A 高」的牌：`river-value` / `river-medium` / `postflop-made-hand`
**全都不展开**，模型只能在没有任何河牌技能的情况下点评河牌决策。

结果：每条街都给了具体点评，河牌那条是

> `[river] ok: 面对 18bb 大注，需要约 36% 胜率，A 高只能抓诈唬，对手诈唬频率未知，弃牌合理。`

赔率是从底池与下注额**推导**出来的（不是编的），也没有出现任何
「超出我的范围」「需要更专业的分析」这类措辞（对全文做过正则检索，零命中）。

**结论：渐进性披露没有让模型变怂。** 第 14 条硬性约束生效了。

#### 验证点 2：没有拿目录的一句话结论硬编细节

对 12 个「只存在于未展开技能正文里」的特征词（`56.3%`、`封锁下注`、`1/4 池`、
`钟形曲线`、`双枪`、`三枪` 等）全文检索，**新旧提示词的输出里都是零命中**。
模型没有把摘要当正文用。

（顺带发现：这个模型本来就不爱堆术语 —— 旧提示词把 14 篇全给了它，它也没引用这些词。）

#### 验证点 3：token 只省了 4.9%（**必须如实说**）

同一手牌、同一份代码，只让技能层不同（新 = 目录 + 展开 8 篇；旧 = 全部 14 篇正文、无目录）：

| | system 字符 | tokens_in |
|---|---|---|
| 新 | 10423 | **6843** |
| 旧 | 10754 | **7196** |
| 差 | −331 | **−353（−4.9%）** |

**为什么这么小**：这手牌展开了 8 篇，只丢掉 6 篇；而目录层（约 830 字）**几乎把这部分节省抵消了**。

这印证了 §9 里字数实测的规律 —— **展开越多，省得越少**：

| 手牌 | 展开 | 相比旧版 |
|---|---|---|
| 翻前开池就结束 | 3/14 | −2949 字 |
| 翻前面对开池后弃牌 | 4/14 | −1601 字 |
| 打到河牌（三条） | 8/14 | −332 字（实测 −353 token） |

**所以本方案的真实收益结构是**：简单手牌省得多，复杂手牌省得少。
「打到河牌、成牌、有对手」这类信息量最大的手牌，省下来的是**注意力的集中**
（少看 6 篇不相关的技能），而不是 token。

> 顺带验证了一件方法论上的事：**字数差与 token 差接近 1:1**（−331 字 → −353 token）。
> 所以 §9 里那些基于字数的估算可以当 token 的可靠代理。

#### 一个待你决定的事：目录层的成本划不划算

目录层约 830 字，在「展开 8 篇」的手牌上几乎抵消了丢技能带来的节省。
如果去掉目录，同一手牌能省约 16% 而不是 5%。

但目录是**验证点 1 通过时在场的**，所以现在没有证据说它不必要 ——
它和第 14 条约束各自贡献了多少，需要再做一次「去掉目录」的对照才知道。
**本期不动**，记在这里。

#### 真机验证顺带抓到的一个 bug

河牌那手牌里 `postflop-draw` 被展开了 —— 河牌持 A 高，被算出了 A2345 的卡顺听牌。
**河牌没有下一张牌，「听牌」不成立。** 已修（`utils/hand_strength.go` 只在公共牌
少于 5 张时才判听牌），并加了回归测试 `TestNoDrawOnRiver`。

这个 bug 只有真机跑才会暴露 —— 单测覆盖不到「这个展开在语义上说不通」这一层。


## 10. 风险与取舍

| 风险 | 影响 | 应对 |
|---|---|---|
| **匹配漏召**（该展开的没展开） | 模型按更弱的依据点评，质量下降且**难以察觉** | S1 等价基线 + 目录层带一句话结论保底 + 命中集写日志 + 定期人工抽查 |
| **匹配误召**（不该展开的展开了） | 变相回到全量注入，注意力重新稀释 | 预算硬顶 + `★` 标记 + §5.5 的截断顺序 |
| **切分维度选错**（最大风险） | 拆得不好会导致每手牌命中 8 篇，等于没拆 | S2 只拆翻前，小步验；实测命中篇数，超过 6 篇说明要合并 |
| **模型拿目录一句话硬编** | 编出不存在的细则 | §8.1 第 13/15 条硬约束 + 需实测（§9.1） |
| **模型以「未展开」推卸责任** | 点评变得畏首畏尾 | §8.1 第 14/16 条 + 需实测（§9.1） |
| **同一手牌两次分析提示词不一致** | `input_snapshot` 失去可复现性，调试被误导 | §5.5 第 3 条：用 `code` 字典序钉死排序 |
| **技能库改了但旧分析没标过期** | 用户看到的是旧依据的结论 | §8.2 升 `PromptVersion`；是否并进 `Stale` 见 §11 |
| **与知识库重复建设** | 两套匹配逻辑漂移 | §7：抽共用纯函数，只写一套 |

---

## 11. 拍板记录（2026-09-21）

| # | 事项 | 决定 |
|---|---|---|
| 1 | §4.3 的 14 篇切分 | **认可**，按该表实施 |
| 2 | `postflop-made-hand` 的触发 | 选 **b**：写轻量牌力分类，**按相对牌力触发**（见 §12） |
| 3 | §8.1 第 14 条「未展开 ≠ 不存在」 | **认可**，写进硬性约束 |
| 4 | `Stale` 口径 | **把 `PromptVersion` 算进去**，但**不自动重跑、不自动扣额度** |
| 5 | 技能正文存储 | **`go:embed` 读 `services/skills/*.md`** |
| 6 | §6.2 预算数值 | 先按目录 900 字 / 正文 4000 字 / 6 篇做，S2 用 `tokens_in` 校准 |
| 7 | 追问路径 | **仍未决**，见 §11.1 |

### 11.1 追问路径：**保持精简版，不做筛选也不全给**（2026-09-21 定稿）

**最终决定**：`BuildChatSystemPrompt` 维持原样 —— 注入 `coachMethodologyBrief`
（698 字精简版常量），不做场景筛选，也不给全部技能。

用户理由：**追问不需要那么多的技能。**

> 这个决定来回改了三次，过程本身有记录价值：
>
> | 次序 | 决定 | 理由 |
> |---|---|---|
> | 1 | 保持现状 | 问题本身已锁定范围，不需要再匹配；匹配要引向量库 |
> | 2 | 给全部技能 | 追问是学习真正发生的地方，答得浅就得不到提升 |
> | 3 | **回滚，保持现状**（定稿） | **追问不需要那么多技能** |

#### 保留的已知状态（**留档，不修**）

| 路径 | 方法论来源 | 字数 |
|---|---|---|
| 分析 | `skills/*.md`（14 篇，按手牌场景展开） | 约 6000~10500 |
| 追问 | `coach_methodology.go` 的 `coachMethodologyBrief` 常量 | 698 |

**`coach_methodology.go` 文件头那句「改一处两边同步，不会漂移」的承诺不成立** ——
分析已改读技能文件，追问还读常量。S1~S3 只让分析受益；以后每加一篇技能，
两条路径的差距继续拉大。**这是接受了的代价，不是遗漏。**

追问那份 698 字当前覆盖：五格形象 / 行动优先级 / 范围推断四线索 / 尺度与赔率的硬数字。

#### 唯一建议补的一条（**未采纳，留作将来可选**）

按「会不会答错」分类，追问缺的内容里只有一类是真问题：

| 类型 | 例子 | 影响 |
|---|---|---|
| **自相矛盾** | 追问「我的 3bet 范围该怎么定？」—— 分析路径有 `core-stance` 里的 §十二 已知边界（**不要凭空替学生生成起手牌范围**），会说"超出本方法论范围"；追问路径没有这条，会**直接编一个范围** | 🔴 同一件事两个答案，且追问那条越界 |
| 只是答得浅 | 追问「翻前开池加多少」—— 分析有「前位 2.5-3.0 倍 / 中位 3.0-3.5 / 后位 3.5-4.0」，追问没有 | 🟡 追问答得笼统，但不算错 |

**最小修法（若将来要动）**：把 §十二 那条约 80 字补进 698 字常量 ——
不涉及路由、不引向量库，只改一个常量。其余缺失可以不管。

#### 关于「真·skills」（模型自主取阅）—— 记录为升级路径，暂不做

现在的实现是**服务端按手牌预判式筛选**，不是 Claude Code 那种「模型读目录 → 自己决定读哪篇」。
后者需要 **tool use 循环**，而当前链路是一次性 `CompleteJSON`。

**分析路径不建议改**：分析的路由键是手牌场景（街道/位置/人数/牌力/行动序列），
全是枚举或可精确计算的值 —— 服务端拿这些结构化字段路由，**比模型自己读文本去猜更准**；
而且分析是每手牌都跑的批量操作，多轮往返的代价大得多。

**追问路径若将来要改**：需要给 `AIClient` 加 tool-call 循环 + 一个 `read_skill(code)` 工具，
system prompt 只放目录不放正文（`RenderSkillCatalog` 已有）。代价是每次追问从 1 次模型调用
变成 2~4 次，且结果不再确定。**触发条件：技能库到 25~30 篇，或追问的 token 成本开始明显。**


---

## 12. 牌力分类（已实现：`utils/hand_strength.go`）

### 12.1 为什么必须是相对牌力

牌力不是手牌自己的属性，而是「它相对于这个牌面有多强」。同一个顺子：

- 在无同花面、无公对的面（`9♦8♣7♥2♥` 上持 `J♠T♠`）—— 只有更大的顺子能赢它，**很强**
- 在同花面且公对的面（`9♠8♠9♣7♠` 上持 `J♦T♦`）—— 对手范围里合理存在同花与葫芦，**只是中等**

绝对牌力算出来两者都是「顺子」，而复盘里的决策（该不该打光筹码）完全不同。
这两条已经写成单测固定下来（`TestStraightOnDryBoardIsVeryStrong` /
`TestStraightOnFlushAndPairedBoardIsMedium`）。

### 12.2 两级结构

| 输出 | 含义 | 用途 |
|---|---|---|
| `Made`（`MadeHand`） | **绝对**成牌等级，12 级 | 路由：`postflop-made-hand` 是否展开 |
| `Tier`（`Tier`） | **相对**档位，6 档 | 口径：该不该打大池 |
| `BoardPlays` | 公牌自己就是这副牌 | 最重的一条降级 |
| `FlushDraw` / `StraightDraw` | 有没有听牌 | 路由：`postflop-draw` |
| `Notes` | 逐条降级原因 | **可审计**：能回溯「为什么这个顺子只算中等」 |

`Notes` 不是调试信息，是设计的一部分：牌力判断会直接影响点评口径，
只给一个档位数字而说不出为什么，出问题时无从查起。

### 12.3 降级规则

先按绝对等级取基准档位，再按牌面结构逐条降级：

| 触发条件 | 影响的成牌 | 理由 |
|---|---|---|
| `BoardPlays` | 全部 | 这副牌人人都有 |
| `ThreeFlush` / `FourFlush` | 顺子、三条 | 对手可能是同花 |
| `Paired` | 顺子、同花 | 对手可能是葫芦 |
| `TripsOnBoard` | 三条 | 公牌自己有三条，hero 的并不比别人强多少 |
| `Connected` | 顶对、超对、两对 | 对手可能是顺子；顶底两对最脆弱 |

### 12.4 `BoardPlays` 为什么不能靠「成牌等级相同」判断

最初实现是「公牌自己的成牌等级 ≥ hero 的等级就算子」。它在下面这种情况下会误判：

> 公牌 `5♠6♦7♥8♣9♠` 已成 9 高顺，hero 持 `T♦J♣` 接成 **J 高顺**。
> 两者等级都是「顺子」→ 旧实现判定 hero 没帮忙 → 把一副准坚果压成「弱」。

正确做法是比**可比较的分值**（`handScore`：等级 + 同类内的比较位），
而不是比等级。已写成回归单测 `TestHeroExtendingBoardStraightIsNotBoardPlays`。

### 12.5 v1 的非目标（刻意不做）

- **踢脚强弱**：顶对好踢与顶对弱踢都算「顶对」。只在「无成牌」一侧区分了超对/顶对/弱对
- **听牌赔率**：只报「有没有听牌」，不算成牌概率与隐含赔率
- **按人数加权**：人数在 `hand` 上而不在牌面上，`utils` 拿不到
- **现代范围平衡与求解器口径**

### 12.6 与技能触发器的共用关系

`AnalyzeBoard` 同时服务两个消费者：
- 技能触发器（`BoardTraits` 维度）
- 相对牌力的降级判断

**必须只有一份实现。** 拆成两份会漂移，后果是「触发器认为这是同花面、
牌力判断认为不是」这种自相矛盾的组合，而且极难查。
