# 德州扑克复盘功能 — 进度与验收文档

> 更新时间：2026-09-15
> 关联文档：[复盘功能设计文档.md](./复盘功能设计文档.md)（设计与取舍的完整说明）
> 涉及仓库：`call-front`（Taro4 + React18 + NutUI）、`call-back`（Gin + GORM + MySQL，模块名 `call-go`）

---

## 一、当前状态总览

| 里程碑 | 内容 | 状态 |
|---|---|---|
| **M1** | 数据模型 + 手牌 CRUD + 列表/详情页 | ✅ 完成 |
| **M2** | 选牌器 + 录入表单 + 详情页回放 | ✅ 完成 |
| **M3** | AI 分析（LLM 客户端 / 提示词 / 异步轮询 / 结果展示） | ✅ 完成，已用真实 DeepSeek 验证 |
| **M3.5** | 「几人桌」+ 位置词表扩展（见 2.2 G） | ✅ 完成，已迁移远程库 |
| **M4** | 长期记忆（洞察累积 + 用户画像页） | ✅ 完成，**未用真实模型验证** |
| **M5** | 追问对话（多轮问答） | ✅ 完成，**未用真实模型验证** |
| **M6** | 统计钻取、成本优化、SSE 流式（H5） | ⬜ 未开始（2026-09-15 明确本轮不做） |

M6 之前的功能链路已经闭合：分析 → 洞察落库 → 画像聚合 → 记忆注入下次分析 → 基于结论追问。
M4/M5 的**提示词与数据链路已用假 LLM 桩验证，但从未真实调用过模型**，见第四节。

---

## 二、验收指引

### 2.1 起环境

```bash
# 1) MySQL（本机手动启动，未注册为开机服务）
/opt/homebrew/opt/mysql/bin/mysqld_safe --datadir=/opt/homebrew/var/mysql &
/opt/homebrew/opt/mysql/bin/mysqladmin ping        # 期望输出 mysqld is alive

# 2) 后端。两种选法见下方「注意」
cd ~/codes/call-back
go run .        # 默认 8080，读 .env

# 3) 前端
cd ~/codes/call-front
npm run dev:h5  # http://localhost:3000，/api 已代理到 8080
```

登录：**远程 dev 库只有 `qwaaaa` / `qwbbbb` 两个账号**（密码不记在文档里），`testuser1 / 123456` 只存在于本地库 `call_game_local_test`。

> **2026-09-15 起，`.env` 指向的远程库 `call_game_dev` 就是本项目的 dev 环境，直接连、直接改。**
> 重启后端会触发 `AutoMigrate`，在那台库上建表 + 按 code upsert 30 行标签种子，这是正常的开发动作。
> 只想在本机验证而不碰远程库时，用环境变量覆盖连接信息（`godotenv.Load()` 不覆盖已存在的环境变量）：
> ```bash
> DB_HOST=127.0.0.1 DB_USER=root DB_PASSWORD= DB_NAME=call_game_local_test SERVER_PORT=8081 go run .
> ```
> 用 8081 而不是 8080，可以同时留着 8080 那个连远程库的实例。

> **注意：后端连着远程库时启动很慢**（约 10~25 秒才监听端口），因为标签种子要逐条 upsert、每次查询 80~170ms 的网络往返。
> 期间 `curl` 返回退出码 `000` 是正常的，别当成启动失败——先看日志里有没有 `Listening and serving HTTP on :8080`。

### 2.2 验收清单

#### A. 入口与 Tab
- [ ] 底部导航变成 **4 个 Tab**：游戏 / 复盘 / 已参与 / 我
- [ ] 点「复盘」进入列表页，标题为「复盘」
- [ ] 右上角有两个按钮：**「画像」（描边）与「+ 记录」（渐变主色）**，画像不该抢记录的视觉权重
- [ ] 位置筛选 **10 个胶囊**（全部/BTN/CO/HJ/LJ/UTG+2/UTG+1/UTG/SB/BB）可左右滑动，**最右边的 BB 能点到**（小程序端重点看这条）
- [ ] 点 `UTG+1` 能筛出结果（查询参数里的 `+` 编码成 `%2B`，见 4.1）

#### B. 录入（核心，请重点验收体验）
- [ ] 点右上「+ 记录」进入录入页
- [ ] 顶部有「几人桌」胶囊（2~9，默认 9 高亮），**位置网格由它驱动**
- [ ] 选位置 BTN；点「选牌」展开 52 张牌网格，选 A♠ 与 K♥
- [ ] **选满 2 张自动收起面板**，已选区显示两张牌
- [ ] 底牌选了 A♠ 后，下方「公共牌」选牌器里 **A♠ 应为灰色不可选**（双向互斥）
- [ ] 展开「翻前」，点「+ 添加行动」→ 默认是「对手 过牌」，改成「我 / 加注 / 2.5」
- [ ] 选 3 张公共牌 → **「翻牌」这条街自动展开**，标题右侧出现底池估算
- [ ] 只选 3 张公共牌，却去「转牌」加一条行动 → 提交时应报错拦住
- [ ] **想法留空直接提交 → 应弹二次确认**（提示没写想法 AI 会泛泛而谈）
- [ ] 填好想法提交 → 提示成功并自动返回列表
- [ ] **填到一半退出录入页，再进来 → 应恢复上次未完成的记录**（草稿保护）
- [ ] 标签输入框可添加/删除标签

#### C. 列表
- [ ] 刚记录的手牌出现在最上方，卡片显示：标题、位置、底牌、公共牌、结果、想法摘要（最多 2 行）、标签
- [ ] 按位置筛选生效（选 CO 只出 CO 的手牌）
- [ ] 2 人桌的手牌，卡片上的位置显示成 **`SB(BTN)`**
- [ ] 下拉刷新正常

#### D. 详情
- [ ] 显示手牌、公共牌（不足 5 张时用虚线空位补齐）、逐街回放（带当时的公共牌）、我的想法、标签、结果
- [ ] 概览行的 meta 显示 **`N人桌 · 单挑/多人池 · N 个对手`**
- [ ] 点「编辑」改想法后保存 → **返回详情页应显示新内容**（不是旧内容）
- [ ] 编辑过内容后，AI 分析区上方应出现**黄色提示「这手牌在分析之后被修改过」**
- [ ] 未分析过的这手牌，底部「问问教练」显示引导文案、**不出现输入框**；分析完成后输入框才出现

#### E. AI 分析（会真实消耗额度）
- [ ] 点「开始分析」→ 弹确认框，显示「今日剩余 N 次」
- [ ] 分析中显示脉冲动画与「通常需要 20~60 秒」
- [ ] 完成后分区展示：一句话概括 / 逐街评价（带 合理·可商榷·有问题 角标）/ 关键错误（做了什么·为什么错·更好线路）/ 替代线路 / 暴露的问题（中文标签 + 严重度 + 引用本手牌的证据）/ **做得好的地方（纯文字，无标签）** / 练习建议 / 免责声明
- [ ] 内容未变时再点「重新分析」→ 提示「内容没变，直接用了上次的结论」，**额度不减少**
- [ ] 当日额度用完后按钮置灰显示「今日次数已用完」

#### F. 边界与安全
- [ ] 用 testuser2 登录 → **看不到 testuser1 的任何手牌**（列表为空，直接访问 URL 也 404）
- [ ] 底牌与公共牌选到重复的牌 → 提交被拦
- [ ] 未登录访问复盘页 → 跳登录
- [ ] 用别人的 hand_id 调 `/reviews/hands/:id/messages` → 拿不到对话

#### G. 几人桌与位置词表（M3.5）
- [ ] 选 9 人桌 → 位置出现 9 个：`SB BB UTG UTG+1 UTG+2 LJ HJ CO BTN`
- [ ] 选 6 人桌 → 只剩 6 个：`SB BB UTG HJ CO BTN`
- [ ] 9 人桌选了 `UTG+2`，再切到 6 人桌 → **位置被自动清空**（不会被静默带到提交）
- [ ] 选 2 人桌 → 只剩两格，SB 显示成 **`SB(BTN)`**，下面一行「单挑时 SB 同时是 BTN」
- [ ] 「关键对手位置」跟着同一个列表变
- [ ] 6 人桌选 UTG+1 提交 → 前端拦住「6 人桌没有「UTG+1」这个位置」
- [ ] 录一手新牌 → 「开始分析」→ 提示词里带「桌型: N人桌」（要花真实额度）

> **词表变更（有存量数据时必读）**：原来的 `MP` 已废弃 —— 它既能读成 LJ 也能读成 HJ，
> 交给模型就是实打实的歧义。词表扩到 9 个位置后，**位置必须属于该人数的集合**，
> 否则后端直接拒收。存量数据由 `database/migrate_20260915_table_size.sql` 迁移（`MP → HJ`），
> 远程库已于 2026-09-15 执行完毕。

#### H. 长期记忆 / 画像（M4）
- [ ] 复盘列表右上「画像」→ 顶部两个数字：已复盘手牌 / 累计漏洞种类
- [ ] 没有总结时显示引导文案；点「重新生成总结」→ 约十几秒后出现一段中文总结，版本号 +1
      （**会真实调用模型，但不占用每日分析次数**）
- [ ] 高频漏洞按出现次数降序；次数徽标按平均严重度配色（黄 → 淡红 → 深红）
- [ ] **点一条漏洞 → 原地展开历史证据**，每条显示 `位置 · 手牌标题 · 日期 · 证据原文`
- [ ] 点某条证据 → 跳到那手牌的详情
- [ ] **展开后的条数应与左边徽标的次数一致**（软删除的手牌两边都不算，见 4.1）
- [ ] 「做对的地方」列出最近 5 条优点，可点进对应手牌
- [ ] 从手牌详情返回画像 → 数据是新的（走 `useDidShow`）
- [ ] 分析第二手牌后，第二次分析的提示词里应出现「这位玩家的历史情况」（M4 记忆注入生效）

#### I. 追问对话（M5）
- [ ] 分析完成后，详情页底部出现「问问教练」输入框
- [ ] 输入「那如果我 4bet 呢？」→ 发送 → 你的消息**靠右紫色气泡**，教练回复**靠左灰色气泡**
- [ ] 连问两轮 → 历史都在，顺序为「问答问答」
- [ ] 发送中按钮 loading、输入框禁用，**快速连点不会发出两条**
- [ ] **故意断网/让后台失败 → 你的输入框内容应保留**（后端是先调模型成功、再把一问一答在一个事务里落库，失败时不留半截对话）
- [ ] 追问花的额度**不占用**每日分析次数
- [ ] 小程序端重点看：输入框聚焦时键盘是否遮挡、发完消息能否自动滚到底

### 2.3 出问题时怎么查

```bash
# 看后端日志（含每次分析的耗时与 token）
tail -f /tmp/callgo_dev_8080.log

# 直接查库
/opt/homebrew/opt/mysql/bin/mysql -u root --default-character-set=utf8mb4 <库名> -e "
SELECT id, status, JSON_LENGTH(result->'\$.leaks') AS leaks, tokens_in, tokens_out,
       duration_ms, error_msg, LEFT(input_snapshot, 200) AS prompt_head
FROM review_analyses ORDER BY id DESC LIMIT 5;"
```

`review_analyses` 表里保留了 **`input_snapshot`（本次实际发给模型的完整提示词）** 和 **`raw_output`（模型原始返回）**。分析结论不对劲时，先看这两列，比猜快得多。

---

## 三、交付清单

### 3.1 后端 `call-back`

**新增**
```
models/review_hand.go              手牌（聚合根，JSON 列存 streets/villains/hero_tags）
models/review_leak_tag.go          漏洞标签字典 + 30 个冷启动标签
models/review_analysis.go          分析记录 + 结构化结果类型 + 提示词版本
models/review_insight.go           洞察原子（M4）+ ProfileLeakStat / ProfileStrengthItem
models/review_profile.go           用户画像模型（M4，含 last_summary_insight_id 水位线）
models/review_message.go           追问对话消息（M5）
models/review_hand_schema_test.go  schema/serializer/标签字典 完整性测试
models/review_analysis_test.go     StrengthItem 契约测试
dto/review.go                      手牌请求/响应
dto/review_analysis.go             分析请求/响应
dto/review_memory.go               画像 / 历史洞察响应（M4）
dto/review_chat.go                 追问请求与消息响应（M5）
services/review_service.go         手牌 CRUD + 标签字典
services/review_analysis_service.go 异步分析编排 / 缓存复用 / 限额 / 结果清洗 / 记忆写入
services/review_memory.go          洞察落库 + 画像聚合 + 总结重写（M4）
services/review_chat_service.go    追问与对话历史（M5）
services/ai_client.go              LLM 客户端（OpenAI 兼容，可换供应商；JSON 与纯文本两种模式）
services/prompt_builder.go         提示词拼装（分析 / 记忆块 / 画像总结 / 追问）
controllers/review_controller.go   15 个接口
utils/hand.go                      牌面规范化与校验 + 位置词表 + 内容指纹
utils/pot.go                       底池推算（给模型看，唯一权威计算）
utils/hand_test.go                 校验 / 位置词表 / 哈希测试
services/review_memory_test.go     聚合 / 重写阈值 / 画像提示词测试（M4）
services/review_chat_test.go       追问上下文 / 历史顺序测试（M5）
services/prompt_builder_test.go    桌型写入提示词的契约测试（M3.5）
config/ai.go                       AI 配置读取（nil 安全）
database/migrate_20260915_table_size.sql  位置词表迁移（MP→HJ + 人数回填）
```

**修改**：`config/config.go`、`database/init.sql`（五张表 DDL）、`main.go`（AutoMigrate + 标签种子）、`routes/routes.go`、`.env.example`、`models/review_hand.go`（位置常量 + TableSize）、`models/review_analysis.go`（提示词版本 → v1.1）

### 3.2 前端 `call-front`

**新增**
```
src/models/types/review.ts         模型 + 前端 + 分析 + 记忆 + 对话 类型
src/models/service/review.ts       接口类型
src/services/reviewApi.ts          15 个接口
src/utils/cards.ts                 牌面纯逻辑（从组件里拆出来，可脱离 UI 测试）
src/utils/poker.ts                 街道文案 / 底池估算 / 位置与桌型
src/components/CardPicker/         选牌器 + 单张牌 + 只读牌列
src/components/StreetActionEditor/ 单条街的行动编辑器
src/components/ReviewHandCard/     列表卡片
src/components/AnalysisPanel/      AI 分析结果展示
src/components/ReviewChatPanel/    追问对话面板（M5）
src/pages/reviews/                 列表页（一级 Tab）
src/pages/review-create/           录入/编辑页 + useReviewForm
src/pages/review-detail/           详情页 + useAnalysis（轮询）
src/pages/review-profile/          画像页（M4）
```

**修改**：`app.config.ts`（4 个页面）、`utils/tabs.ts` + `components/BottomTabBar.tsx`（3 Tab → 4 Tab）、`components/index.ts`、`models/{types,service}/index.ts`、`models/transformers.ts`、`services/api.ts`、`tsconfig.json` + `types/global.d.ts`（见 4.3）

### 3.3 接口一览

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/reviews/hands` | 创建手牌 |
| GET | `/api/v1/reviews/hands` | 列表（position/tag/game_id/analyze_status/keyword 筛选） |
| GET | `/api/v1/reviews/hands/:id` | 详情 |
| PUT | `/api/v1/reviews/hands/:id` | 更新（整体替换） |
| DELETE | `/api/v1/reviews/hands/:id` | 删除（软删除） |
| POST | `/api/v1/reviews/hands/:id/analyze` | 触发分析（异步，立即返回 pending） |
| GET | `/api/v1/reviews/hands/:id/analyses` | 该手牌的历史分析（带 `stale` 标记） |
| GET | `/api/v1/reviews/analyses/:id` | 轮询状态与结果 |
| GET | `/api/v1/reviews/leak-tags` | 标签字典 |
| GET | `/api/v1/reviews/ai-status` | AI 是否可用 + 今日剩余额度 |
| GET | `/api/v1/reviews/profile` | 我的画像（M4，**会顺手重算统计**，见 4.1） |
| POST | `/api/v1/reviews/profile/summary/refresh` | 手动重写画像总结（M4，同步，会调用模型） |
| GET | `/api/v1/reviews/insights?tag_code=xxx&limit=50` | 某漏洞的历史证据（M4 钻取；不传 tag_code 返回全部漏洞洞察） |
| POST | `/api/v1/reviews/hands/:id/messages` | 追问（M5，同步返回一问一答） |
| GET | `/api/v1/reviews/hands/:id/messages` | 对话历史（M5，按时间升序） |

---

## 四、验证到什么程度（重要）

### 4.1 实现细节与踩过的坑

这几处不做说明的话，下次很容易改回去：

| 点 | 说明 |
|---|---|
| **`+` 必须编码** | 位置 `UTG+1` 作为查询参数时，不编码的 `+` 会被解析成空格。前端用 `URLSearchParams`（自动编成 `%2B`），实测：`?position=UTG%2B1` 命中 1 条、`?position=UTG+1` 命中 0 条。以后新增带 `+` 的位置同样要小心 |
| **`GET /reviews/profile` 会写库** | 它顺手重算一次统计。漏洞排行是洞察表的派生数据，重算只是 3 次查询，换来「画像页永远反映当前洞察表」。语义上等同刷新物化视图，不是「GET 顺手改业务数据」 |
| **软删除的手牌两边都不算** | 洞察引用的是 hand_id，手牌软删后 GORM 查不到它。若聚合时不加过滤，画像会显示「出现 4 次」而钻取只能列出 3 条，数字对不上。`RefreshProfile` 与 `ListInsightsByTag` 都按存活手牌过滤，保证「出现 N 次」与点进去的条数严格一致 |
| **追问是先调模型、后落库** | 模型失败时不写任何一条。若先把用户消息存下，历史里会留一句没有回复的话，下一轮喂给模型它会以为自己没答完 |
| **画像总结是增量修订，不是重写** | 提示词里带上「已有总结 + 标签统计 + 新增洞察」。每次从零重写会让总结随最新一手牌剧烈摆动，用户会觉得「我的总结怎么天天变」 |
| **`BuildChatPrompt` 用单轮 + 历史拼进 user 段** | `AIClient` 只有 system+user 两段，没有多轮 messages 数组。历史按「学员：/你：」前缀正序拼进去，倒着喂模型会把它自己说的话当成用户说的 |
| **前端的 `process.env` 是构建期替换** | 小程序/浏览器里没有 Node 运行时。`types/global.d.ts` 只声明了 `process.env`，**没有引入 `@types/node`**——那会把整套 Node 全局塞进小程序代码的类型环境，反而掩盖问题 |

### 4.2 已验证 ✅

| 项 | 方式 |
|---|---|
| 后端编译 / vet / **45 个测试** | `go build` `go vet` `go test` 全绿 |
| JSON 列在真实 MySQL 上是 `json` 类型 | 建表后 `SHOW COLUMNS` + `JSON_TYPE` 核对 |
| `serializer:json` 真能序列化 | 直接调用序列化器 + 落库后查 `JSON_TYPE=OBJECT` |
| 手牌 CRUD / 校验 / 跨用户隔离 / 软删除 / 筛选 / 分页 | 真实 HTTP 接口分两批逐项断言，共 30 余项 |
| 异步分析全链路 | 自建**假 LLM 桩**覆盖：成功 / 复用 / 白名单过滤 / 围栏剥离 / 解析失败 / 500 重试 / 401 不重试 / 超时 / 限额耗尽 |
| 限额耗尽时**不调用模型** | 桩调用次数为 0，确认在入口拦截 |
| 提示词内容 | 逐项断言（防注入包裹 / 标签表 / 底池 / 手牌格式）**10 项** |
| **真实 DeepSeek 调用** | 输出合规 JSON、只用字典内标签、越界标签走 suggestedTags、keyMistake 只一个、不编造 EV、**赔率与我的底池推算完全一致** |
| 前端 TypeScript | `tsc --noEmit` **整体零错误、退出码 0**（2026-09-15 起才做得到，见 4.3） |
| 前端构建 | **H5 与微信小程序两个目标都构建通过** |
| 位置词表的正确性（M3.5） | `TestPositionsForTableSize` 逐人数逐位置断言顺序与数量，与前端 `positionsForTableSize` 是同一份契约 |
| 人数与位置的匹配校验 | 真实 HTTP：6人+LJ 拒、9人+UTG+1 过、9人+MP 拒、不传人数默认 9、人数 10 拒、6人桌+对手 UTG+2 拒 |
| 位置词表迁移脚本 | 本地库造了两行「老数据」实测：`hero_position` 的 MP→HJ、`villains` JSON 里的 `"MP"`→`"HJ"`、非 MP 行未被误动；远程库已执行（该库无 MP 数据，脚本幂等地什么都没改） |
| 画像聚合（M4） | 单元测试覆盖分组/计数/平均严重度/排序/证据截断/标签改名兜底/空输入；真实 HTTP 验证 `(3+2+3+3)/4=2.75` 等数值 |
| 画像钻取一致性（M4） | 真实 HTTP：造了一行软删除手牌，确认「出现 N 次」与钻取条数都是 3 条且都有标题 |
| 追问全链路（M5） | **假 LLM 桩**（临时 Python 服务，跑完已删）：正常追问返回一问一答、多轮历史顺序正确、无分析时拒、空内容拒、越权拒 |
| 记忆注入提示词（M5 验证 M4） | 桩收到的提示词里确认含「这位玩家的历史情况」（M4 的记忆块）+「桌型: 6人桌」（M3.5）+ 分析结论 + 对话历史 |

### 4.3 未验证 ⚠️

| 项 | 影响 | 怎么补 |
|---|---|---|
| **M4/M5 从未真实调用过模型** | 提示词与数据链路是验过的，但**模型拿到画像后会不会真的保持与之前结论一致、总结写得像不像人话，完全没看过** | 用真实 Key 跑一轮「分析一手牌 → 追问一次」，约消耗 2 次调用 |
| 小程序端交互细节 | 横向滚动、`showModal`、`Textarea`、追问输入框的键盘遮挡在小程序里与 H5 有差异 | 用微信开发者工具打开 `dist/` |
| 并发场景 | 两个用户同时分析、重复点「分析」的幂等性；画像聚合与总结重写并发写同一条 profile | 需要压测 |
| 提示词效果调优 | 所有分析结论都是桩产出的，真实质量未知 | 多用几十手牌看 |
| 画像首屏空态 | 分析第一手牌时会**同时**触发一次总结重写（多一次模型调用），这个成本换「画像页不是空的」，是不是划算还没验证过 | 真实跑一轮看体验 |

> 2026-09-15 更新：**H5 已用 Edge 实际渲染验证**（录入页与详情页），量测结果为：
> 文档宽 = 视口宽（无横向溢出）、内容区 `overflow-y: auto` + `box-sizing: border-box` 且真的能滚动、
> 13 列牌面网格无溢出、底部按钮完整在视口内、详情页标题居中偏移 0px、返回按钮点击穿透正常。
> 但验证脚本是**一次性的、放在 /tmp 且已删除**，没有沉淀成 e2e 用例。若以后要长期回归，需要另建测试目录。

### 4.4 验证过程中抓到的真实缺陷（M1~M3，都已修）

1. **`Updates(map)` 绕过 GORM 字段序列化器** —— `serializer:json` 对 map 形式的更新不生效，分析结果永远写不进去，且错误处理只记日志就继续，把状态标成 `done`，造成「手牌显示已分析但没有任何结论」。改用 `Save()`，且写成功才同步状态。
2. **数据竞争** —— 后台 goroutine 修改的对象正是 HTTP 响应正在序列化的对象。改为传值拷贝。
3. **`NewAIClient()` 在配置未加载时 nil 解引用崩溃** —— 被项目原有的 `routes` smoke test 抓到。改为惰性读配置。
4. **小程序里位置筛选滚动失灵** —— `View` 的 `overflow-x:auto` 在小程序不滚动，改用 `ScrollView`，否则最右边的 BB 点不到。
5. **草稿被空表单覆盖** —— 恢复草稿与自动保存两个 effect 赛跑。加 hydrate 闸门。
6. **详情页编辑后显示旧内容** —— 加 `useDidShow` 刷新。
7. **底牌/公共牌可选出重复的牌** —— 选牌器双向互斥。
8. **`10s` 被规范化成 `s`** —— 用户习惯写 `10` 而非 `T`，测试抓到的静默丢字符。

### 4.5 验收阶段修掉的缺陷（2026-09-15）

验收反馈：「记录一手牌页面，内容区域无法滚动，并且内容区域靠右显示了，部分被遮挡了」。根因是两个布局错误，且 `PageLayout` 里早就写好了正确写法与警告注释：

| 症状 | 根因 | 修法 |
|---|---|---|
| 内容区滚不动 | 自己写的 `overflow: hidden`，且缺 `min-height: 0`（flex item 默认 `min-height:auto` 会被内容撑开） | 改用项目已验证的 `PageLayout`，骨架交给它 |
| 内容靠右、右侧被裁 | 缺 `box-sizing: border-box`，16px 内边距额外撑出 32px 宽度 | 同上；`PageLayout` 已声明 box-sizing（全局 `* { box-sizing }` 在 H5 产物中不生效） |

同批修掉的还有：

- **`CardPicker` 的 13 列网格会横向溢出**：`repeat(13, 1fr)` 的 `1fr` 最小值是 `min-content`，且单元格的 1px 边框在缺 box-sizing 时会让每格比列宽多 2px（13 列累计 26px）。改为 `repeat(13, minmax(0, 1fr))` 并显式声明 box-sizing。
- **`.selected-list` 缺 `min-width: 0`**：可能把右侧「选牌」按钮挤出屏幕。
- **详情页头部「编辑」「删除」被压成竖排**（截图发现）：`PageHeader` 的 `.header-right` 是固定 60px（为单个图标设计的），放两个文字按钮就被压成「编/辑」。只在本页放开宽度，并改用把标题绝对定位横跨整宽的方式保持居中（不动公共组件，其他页面依赖那 60px 对称）。
- **`review-detail` 的 `ScrollView` 未使用**：改用 `PageLayout` 后成了未使用导入。

### 4.6 M3.5 / M4 / M5 阶段修掉的缺陷（2026-09-15）

这批里有 **3 个是前端 `tsc` 报错暴露出来的真 bug**，不是类型噪音：

| 缺陷 | 症状 | 修法 |
|---|---|---|
| **交易流水的用户筛选一直失效** | 前端传 `{userId}`，而接口层与后端读的都是 `user_id`，参数被静默丢弃 → 后端 `if userID > 0` 不成立 → **返回全场所有人的流水**。触发路径是活的：存分/取分成功后都会按用户重新加载 | `store/useTransactionStore.ts` 改传 `{user_id: userId}` |
| **登录按钮的 `htmlType` 不是 NutUI 的 prop** | `ButtonProps` 里只有 `nativeType`。H5 上它一直"能提交"是因为 `<button>` 不带 `type` 时 HTML 默认就是 submit —— 歪打正着；**小程序端没验证** | 改用 `nativeType="submit"` |
| **`Cell` 的 `isLink` 不存在** | `CellProps` 只有 `align/clickable/description/extra/onClick/radius/title`。**右侧箭头从未渲染过**，那两行菜单看着和普通行一样 | 删掉假属性（想要箭头得换 `extra` 写法，未做） |
| 画像计数与钻取条数对不上 | 软删除手牌的洞察仍计入排行，但钻取页面查不到它 | 聚合与钻取两处都按存活手牌过滤（见 4.1） |
| 画像页永远显示旧数字 | 统计只在分析完成时刷新，删手牌/标签改名后会一直显示上次的数字 | `GET /reviews/profile` 顺手重算（见 4.1） |
| `Store` 里 `joinGame` 多传一个参数 | `joinGame` 只收 `gameId`，加入者身份由后端 JWT 决定，多传的一直被忽略 | 去掉第二个实参 |

**`tsc` 从「永远非零退出」变成可用门禁**：
项目原先**没有 `typecheck` 脚本**，`tsc --noEmit` 从未进过流程，所以上面这些 src 报错能一直存活。
剩下 73 个报错全部来自 `@tarojs/*` 自己的 `.d.ts`（引用了未安装的 `react-native` 类型与 `CommonEventFunction`），
加 `skipLibCheck: true` 后整体归零。代价是它也跳过所有 `.d.ts` 自身的检查（调用点仍照常校验）。
**仍未加 `typecheck` 脚本**，批报错随时可能再长回来。

---

## 五、待决事项

### 5.1 需要你确认

1. **`product.md` 与现状不符**（从 M1 挂到现在）
   `product.md` 要求所有接口调用用 ahooks 的 `useRequest`，但 `package.json` **没有安装 ahooks**，
   现有页面（含复盘全部页面）都用 `useState` + `useCallback` + 直接调 API。
   设计文档 §4.4 早就指出这点并建议修订，一直没动。要改吗？

2. **要不要加 `typecheck` 脚本**
   本轮把 `tsc --noEmit` 清到了零，但项目里**没有 `typecheck` 脚本**，也没挂进 `build` 前置。
   不加的话这批报错会像上次那样慢慢长回来（见 4.3）。

3. **是否跑一轮真实模型验证**
   M4/M5 的提示词与数据链路都用假桩验过，但**从未真实调用过模型**。
   主要未知：模型拿到画像后会不会保持与之前结论一致、总结写得像不像人话、
   首手牌就触发总结重写（多一次调用）划不划算。

### 5.2 已知设计取舍（非缺陷，但值得知道）

- **画像里的「优点」不做聚合计数**。设计文档 2.8 写的是 strengths 与 leaks 同构（带 tag_code/count），
  但 M3 最终把优点定成了纯文本不带标签（`models/review_analysis.go` 的注释写明「优点本来也不需要聚合」）。
  所以画像里存的是「最近 5 条」而非计数排行。**要按文档来，得先给优点建标签字典。**
- **画像重写用的是纯文本补全，不走 JSON 模式**。`response_format=json_object` 会让模型把总结写成字段化短句，读起来不像人话。
  为此把 `AIClient` 拆出了 `CompleteJSON` / `Complete` 两种模式，共用同一套重试与错误处理。

- **底池是估算值**，不计盲注与前注。UI 标明了「估算」，且**不落库**（`StreetRecord.PotStartBB` 保持为空），只在提示词里给模型做赔率推理用。若要精确底池，需要录入盲注与前注。
- **AI 不提供精确 EV / 胜率**。没有求解器就不暗示精确性，提示词里明确禁止编造 EV 数字，UI 也有免责声明。
- **标签字典只有「漏洞」没有「优点」**，所以 strengths 设计成纯文本不带标签（这是本次最后改的一处）。
- **`streets` 用 JSON 列而非拆表**。手牌是聚合根，永远整手读写；代价是无法按行动细节做 SQL 聚合（"我在河牌被 check-raise 时的弃牌率"这类统计做不到，属于 M4 之后的事）。
- **v1 只记录一个关键对手**，其余归入「其他人」聚合角色。
- **内容不变时复用上次结论**，不重复调用模型、不消耗额度。改标题不影响指纹（重命名不会让已有分析失效），但改任何手牌内容都会让指纹变化并把 `analyze_status` 重置为 `none`。

---

## 六、下次继续时的起点

M1~M5 已全部完成。下一步是 **M6（打磨）**，设计文档 §7 给的三件事：

| 子项 | 说明 | 现在的基础 |
|---|---|---|
| **统计钻取** | 按标签/位置/街道的漏洞分布；「我在河牌被 check-raise 时的弃牌率」这类聚合 | `review_insights` 已有 `tag_code`/`hand_id`/`severity`，按位置/街道聚合需要连 `review_hands`，或用 `streets` JSON 做 `JSON_CONTAINS`（做不到复杂聚合，见 5.2 的取舍） |
| **成本优化** | 现在每次分析 + 每次总结重写 + 每次追问都是独立调用；提示词缓存、`content_hash` 已有，但**只有分析走缓存**，追问和总结没有 | `AIClient` 已支持 JSON / 纯文本两种模式；`review_analyses` 有 `tokens_in/out` 记账，追问的 token 记在 `review_messages` 上 |
| **SSE 流式（仅 H5）** | 小程序不支持 SSE，v1 两端统一轮询 | 现有轮询在 `pages/review-detail/useAnalysis.ts`；追问目前是同步接口，要走流式得改成异步 + 推送 |

如果 M6 之前要做真实模型验证，优先验这三条：
1. 分析第二手牌时，画像是真的注入了（看 `review_analyses.input_snapshot` 里有没有「这位玩家的历史情况」）
2. 总结重写产出的中文质量（`review_profiles.summary`）
3. 追问时模型会不会推翻自己之前的结论

### 环境备忘

**进程与库**
- 后端仓库在本机是 **`~/codes/call-back`**（模块名 `call-go`）。`CLAUDE.md` 里写的是 `～/codes/call-go`——注意那是**全角波浪号**，路径无法解析，且该目录并不存在。
- **8080 上跑的是连远程 dev 库 `call_game_dev` 的实例**，2026-09-15 已用 M4/M5 代码重启过。
  8090 上另有一个更早的实例，**跑的是旧二进制**，用它调试记得重启。
- 杀本机后端实例时**按 PID 精确杀**，不要用 `pkill -f "exe/main"` 这类模糊匹配——它会连带杀掉你 IDE 里跑的那个实例。
- 本地 scratch 库 `call_game_local_test` 里留着联调用的测试数据（8 条洞察、1 条伪造分析、4 条对话消息、若干手牌），要清可以清。

**本机 MySQL**
- 26.7 由 Homebrew 安装（`brew uninstall mysql` 可卸，但不会删 `/opt/homebrew/var/mysql` 的数据目录），**手动启动、未注册为开机服务**。
- Homebrew 直连 `ghcr.io` 只有 ~45KB/s，装大包时用国内镜像（清华/南大实测 4.2~4.6MB/s）：
  ```bash
  HOMEBREW_BOTTLE_DOMAIN=https://mirror.nju.edu.cn/homebrew-bottles brew install <包名>
  ```

**密钥**
- **`.env` 里放真实 DeepSeek Key，`.env.example` 必须留空**。曾发生过 Key 被填进 `.env.example`（该文件被 git 跟踪）的情况，幸好未提交；若已提交则只能吊销重发。
