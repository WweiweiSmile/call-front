# 赔率快速训练（Odds Drill）设计文档

> 版本 v1.0 ｜ 编写日期：2026-09-24 ｜ 设计文档（记录**为什么这么设计**）
> 前置文档：[复盘功能设计文档.md](./复盘功能设计文档.md)、[教练技能库设计文档.md](./教练技能库设计文档.md)
> 关联项目：`call-front`（Taro4 + React18 + NutUI）。
> M1~M4 的实现**没有改 `call-back`**；2026-09-24 按你的要求做了一次技能层的
> 口径订正（§2.8），那是本方案唯一动到后端的部分。
>
> 本文档是**动工前的设计**。§8 的待确认事项已于 2026-09-24 拍板，见 §9。
>
> **口径已全部对拍**（2026-09-24）：唯一公式在 `odds-table.md` 的四张表上逐格复算，
> 表 1（11/11）、表 2（16/16）、表 3（8/8，走取整口径）、表 4（12 格中 11 格）一致。
> 三处需要说明的地方：表 3 必须走**文档取整口径**（§2.5）、
> 表 4 的卡顺那一格是**文档自身的内部不一致**（§2.4）、
> §十 推论② 是**过度概括**（§2.7）。
> **三处已于 2026-09-24 全部订正进 `odds-table.md`**，代价是常驻技能 +265 字，见 §2.8。
>
> **进度**：**M1 ✅ / M2 ✅ / M3 ✅ / M4 ✅**（2026-09-24）。`npm run check:odds` **16 项全绿**，
> e2e **9 条全绿**。入口、五种题型、四档难度、填空作答、进度本地存储、错题重练
> 已全部闭环 —— **本方案的四个里程碑全部完成**。
>
> 遗留待你定：§8 第 6 条（本地怎么跑 e2e —— 本机只装了 Edge，没下 playwright 的 chromium）。

---

## 0. 一句话

把常驻技能 `odds-table` 里的算术练成肌肉记忆：给一个局面，**3 秒内报出「需要多少胜率」和「这个跟注是对是错」**。

---

## 1. 背景与目标

### 1.1 为什么现在做

`odds-table` 是**常驻技能**（`教练技能库设计文档.md` §4.2），每手牌的分析都在用它 ——
教练给出的「1/3 池约需 20% 胜率」「转牌同花听只剩 20%，刚好卡在 1/3 池门槛上」
这类结论，正是这套算术。但**这套算术目前只出现在 AI 的输出里**，
用户自己算不快，就只能被动接受结论。

训练器把这块反过来：**让用户先算，再对答案**。算得快了，复盘时看 AI 的结论会更快，
也才有能力质疑它。

### 1.2 目标

1. 用户能在 3 秒内报出**常见尺度的门槛值**（1/3 池 20%、1/2 池 25%、1 倍池 33%……）。
2. 用户能报出**常见听牌的实际胜率**（同花听 9 outs 双街 35% / 单街 20%……）。
3. 用户能判断**一个跟注是对是错**（门槛 vs 实际胜率）。
4. 训练进度本地可查（正确率、连击、错题），关掉小程序不丢。

### 1.3 非目标

见 §6。一句话：**不做**牌面识别、不做精确 equity 枚举、不做后端落库。

---

## 2. 口径：与 `odds-table.md` 对齐（本方案的根）

这是整个方案最重要的一节。训练器的答案**必须**和教练嘴里说出来的数一样，
否则「你练的」和「教练说的」会互相打脸 —— 这种不一致比没有训练功能更伤。

### 2.1 唯一公式

> **需要胜率 = 跟注额 ÷ 跟注后的底池**

`odds-table.md` 的附注原话是「需要胜率 = 跟注额 ÷ 跟注后的底池，未计抽水」。
这一个式子能推出文档里全部四张表，所以引擎里**只实现它一个**，其余都是它的变形。

以「面对 1/2 池下注」为例：池 1，对手下 0.5 → 池 1.5；我跟 0.5 → 跟注后池 2.0；
需要胜率 = 0.5 / 2.0 = **25%** ✓（与文档表 1 一致）

三个推得的变形：

| 场景 | 设 | 需要胜率 |
|---|---|---|
| 对手下注 b 倍池，我跟 | 池 = 1 | `b / (1 + 2b)` |
| 我下注 b 倍池，对手加注到 X·b 倍 | 池 = 1 | `b(X−1) / (1 + 2Xb)` |
| 反查：我有 eq 胜率，最多能跟多大 | —— | `b = eq / (1 − 2eq)`，`eq ≥ 50%` 时无上限 |

双边口头换算（两套说法都是德州圈里的常规表达）：

- **赔率** `x:1 = (1 − eq) / eq`，例：需要 25% → 赔率 3.0:1
- **BEP 反算** `eq = 1 / (赔率 + 1)`，即文档开头那句 `BEP = 1/(底池赔率+1)`

### 2.2 逐格对拍结果（2026-09-24 已跑）

用 `node` 复算了 `odds-table.md` 的每一格：

**表 1（面对下注）11/11 全中：**

| 下注 | 1/4 | 1/3 | 1/2 | 2/3 | 3/4 | 1 | 1.5 | 2 | 3 | 5 | 10 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 文档 | 17 | 20 | 25 | 29 | 30 | 33 | 38 | 40 | 43 | 45 | 48 |
| 复算 | 16.67 | 20.00 | 25.00 | 28.57 | 30.00 | 33.33 | 37.50 | 40.00 | 42.86 | 45.45 | 47.62 |
| | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |

**表 2（我先下注、对手加注）16/16 全中：**

| 我的下注 | 加注 2 倍 | 2.5 倍 | 3 倍 | 4 倍 |
|---|---|---|---|---|
| 1/3 池 | 14.29 → **14** | 18.75 → **19** | 22.22 → **22** | 27.27 → **27** |
| 1/2 池 | 16.67 → **17** | 21.43 → **21** | 25.00 → **25** | 30.00 → **30** |
| 2/3 池 | 18.18 → **18** | 23.08 → **23** | 26.67 → **27** | 31.58 → **32** |
| 1 倍池 | 20.00 → **20** | 25.00 → **25** | 28.57 → **29** | 33.33 → **33** |

（格式：复算值 → 文档取整值。全部落在四舍五入内。）

**表 4（听牌换算）12/12 中**（卡顺那一格 2026-09-24 由 17% 订正为 16%，见 §2.4）：

| 听牌 | outs | 双街复算 | 文档 | 单街复算 | 文档 |
|---|---|---|---|---|---|
| 卡顺 | 4 | 16.47% | 16% ✓（订正前写的是 17%） | 8.70% | 9% ✓ |
| 两张高牌 | 6 | 24.14% | 24% ✓ | 13.04% | 13% ✓ |
| 两头顺 | 8 | 31.45% | 31% ✓ | 17.39% | 17% ✓ |
| 同花听 | 9 | 34.97% | 35% ✓ | 19.57% | 20% ✓ |
| 同花听 + 卡顺 | 12 | 44.96% | 45% ✓ | 26.09% | 26% ✓ |
| 同花听 + 两头顺 | 15 | 54.12% | 54% ✓ | 32.61% | 33% ✓ |

用的是精确组合数（不是四二法则）：

- **单街**（转牌→河牌，未见 46 张）：`outs / 46`
- **双街**（翻牌→河牌，未见 47 张）：`1 − C(47−outs, 2) / C(47, 2)`
- **四二法则**：翻牌 `outs × 4`、转牌 `outs × 2`。文档自己注明「outs ≥ 15 张时会高估」——
  15×4 = 60%，精确值 54%，确实高估。**所以四二法则是口诀，不是答案**（见 §3.4）。

### 2.3 修正：你说的第二问，`odds-table.md` 里其实已经有

我在讨论时说过「对手下注→我加注→对手需要多少胜率」这张表文档里没有。
**这个说法是错的，先纠正。**

文档表 2 的表头是「**我先下注、对手加注**，倍数 = 对手加注后的总额 ÷ 我的下注额」，
但要算的那件事是**同一件**：*最后一个加注者之外的那个人，要跟多少、跟完池多大*。

把表 2 从对手的座位读一遍，就正好是你的第二问：

```
表2：我下注 b，对手加注到 X·b      → 我需补 b(X−1)，池 = 1 + 2Xb
你的第二问：对手下注 B，我加注到 R  → 对手需补 R−B，池 = 1 + 2R
```

两式在 `b := B`、`X := R/B` 下**完全同构**，算出来的数一模一样。
举例：对手下注 1/2 池、我加注到 2 倍（= 1 倍池），对手需补 0.5，跟注后池 3.0
→ 需要 **17%** —— 查文档表 2「我的下注 1/2 池 / 加注到 2 倍」那一格，正是 17%。

**结论：不需要给 `odds-table.md` 补表**，缺的只是「换个座位读」这件事没写明。
（要不要顺手在文档里加一句「本表对调双方座位后同样适用」，见 §8 第 4 条。）

### 2.4 卡顺那一格：文档自身的一处内部不一致（**2026-09-24 已订正**）

文档表 4 给「卡顺 4 outs / 双街 / 17%」，精确组合数是 **16.47%**。

我第一版设计里说「四舍五入到 17% 说得通」—— **这句是错的**，实测四舍五入给的是 **16%**：
16.47 既不是 17，也不是 17 的取整来源。所以这一格不是取整问题，
**是文档自身的一处不一致**。旁证：文档 §十 自己的表写着「1/4 池 → 翻牌需要 outs 5」，
而 4 张 outs 算出来 16.47% < 1/4 池门槛 16.67% —— **§十 与精确值自洽，表 4 那一格才是不自洽的**。

它会翻掉一个结论：

- 1/4 池的门槛 = **16.67%**
- 卡顺双街精确值 = **16.47%** < 16.67% → 严格说不够跟
- 但文档表 4 的 17% > 16.67% → 读起来刚好够跟

**拍板结果**：判分用**精确值 + ±1.5pp 容差**，解释卡点名这个边界。
这个选择顺带把冲突化解了 —— 16%（取整）和 17%（文档）距精确值分别是 0.47pp 和 0.53pp，
**两种答法都在容差内，都判对**。

> **2026-09-24 更新**：文档那一格已订正为 **16%**（取整值），于是
> `check:odds` 的表 4 从「11/12 格 + 1 处例外」变成 **12/12 格全部按等式比较** ——
> 断言比之前更强。见 §2.8。

### 2.5 表 3（反查）必须走「文档取整口径」——**这是设计后才验出来的**

反查表在**精确口径**下有 2 行对不上文档：

| 我的胜率 | 文档 | 精确口径反解 | 差在哪 |
|---|---|---|---|
| 33% | 1 倍池 | **3/4 池** ✗ | 1 倍池精确门槛 33.33% > 33% |
| 45% | 5 倍池 | **3 倍池** ✗ | 5 倍池精确门槛 45.45% > 45% |

原因和卡顺同源：**文档两头都在取整**（表 1 把 33.33 写成 33、45.45 写成 45），
于是「整数胜率 → 档位」的边界就互相打架了。

走**文档取整口径**（先把候选尺度的门槛四舍五入成整数百分比，再和取整后的目标胜率比）
则 **8/8 全中**。训练器的答案要跟教练说的话一致，所以判分走取整口径。

引擎里因此有**两个**反查函数，分工不同，别混用：

| 函数 | 口径 | 用途 |
|---|---|---|
| `maxCallFraction(eq)` | 精确反解 `eq/(1−2eq)` | 解释卡里算到小数点后的真实边界 |
| `largestCallable(fractions, eq)` | 文档取整口径 | T4 的判分（离散档位选择） |

> **2026-09-24 更新**：这套口径已写进 `odds-table.md` 的「附」开头，见 §2.8。

### 2.6 容差 ±1.5pp 是有依据的，不是拍脑袋

文档每一格都取整成了整数百分比，取整带来的偏差**实测上限 0.50pp**
（2026-09-24 订正卡顺之后，最大的变成表 1 的 1.5 倍池：37.50 → 38；
订正前是卡顺那一格的 0.53pp）。

所以容差取 **1.5pp** 时，**「照着文档答」永远判对**。
这条性质由 `check:odds` 的「容差覆盖」一项常驻守着 —— 文档将来多加一档更大的尺度、
取整偏差超过 1.5pp 时，脚本会红。

---

### 2.7 文档 §十 推论② 是过度概括：只对 9 张 outs 及以下成立（**M3 才验出来**）

文档 §十 的推论② 原文：

> 对手转牌下注达到或超过 1/2 池时，用**任何**听牌跟注都是错误

这句话**不成立**。转牌面对 1/2 池的门槛是 25%，而各听牌的单街胜率是：

| 听牌 | outs | 单街胜率 | 门槛 25% |
|---|---|---|---|
| 卡顺 | 4 | 8.70% | 跟注是错的 |
| 两张高牌 | 6 | 13.04% | 跟注是错的 |
| 两头顺 | 8 | 17.39% | 跟注是错的 |
| 同花听 | 9 | 19.57% | 跟注是错的 |
| **同花听 + 卡顺** | **12** | **26.09%** | **够跟 → 推论② 错了** |
| **同花听 + 两头顺** | **15** | **32.61%** | **够跟 → 推论② 错了** |

**文档自己就反驳自己**：表 4 的 12/15 outs 两行，单街胜率写的是 26% / 33%，
「双街可跟的最大下注」一栏还标着「任意尺度 / 直接加注或全下」。
准确的表述应该是「**9 张 outs 及以下的**听牌跟注都是错误」。

**处理方式**（当时按 §8 第 4 条先不动后端；**2026-09-24 已按你的要求订正，见 §2.8**）：

- **T5 按数学判对错** —— 组合听牌就是该跟。训练器不能教一条错的规则。
- 解释卡按情况分开说话：撞上 ≤9 outs 的说「经验法则：……（文档 §十 推论②）」，
  撞上组合听牌反例的明说「那句话只对 9 张 outs 及以下成立，这里该跟」。
- `check:odds` 里加了一条常驻检查「推论② 的真实边界（≤9 outs）」，
  把 4 个成立项与 2 个反例都钉死。**将来谁"照推论②"改了出题逻辑，脚本会红。**
- 这一条也意味着 §7.1 那份「改技能层要回查的清单」有三处文档级发现：
  表 4 卡顺（§2.4）、表 3 反查口径（§2.5）、§十 推论②（本节）。
  **三者已于 2026-09-24 全部订正进后端文档，见 §2.8。**

---

### 2.8 三处订正已落进 `odds-table.md`（2026-09-24）

按你的要求改了后端技能正文。**只动文字范围与一个数字，没有动任何表格结构。**

| 对应 | 改了什么 | 前 → 后 |
|---|---|---|
| §2.4 | 表 4 卡顺 / 双街 | 17% → **16%**（精确 16.47%，取整就是 16）；同行的「1/4 池」→「约 1/4 池」 |
| §2.5 | 表 3 的口径 | 「附」开头补一句「表内百分比一律取整到整数，比较也按取整后的整数比」，并点名反查表最依赖这条 |
| §2.7 | §十 推论② 的范围 | 「用**任何**听牌」→「用 **9 张 outs 及以下**的听牌」，括号里写明组合听牌这个反例 |

**代价：常驻技能 2000 → 2265 字（+265），常驻合计 3280 → 3545 字。**
odds-table 是常驻技能，这 265 字**每一手牌都要付**。第一版订正块写到 +493，
压到 +265 —— 理由是**技能正文是给模型读的，变更记录不该塞进去**：
模型只需要正确的规则，来龙去脉留在本文档里。

**一个附带好处**：卡顺订正后，`check:odds` 的表 4 从「11/12 格 + 1 处例外」
变成 **12/12 格全部按等式比较**，断言比之前更强。

**回查清单**（按 `教练文档漂移回查点` 的经验）：

- [x] `~/codes/call-back/services/skills.go` 的 `LogSkillLayerSizes` 注释：
      常驻字数 2000/3280 → 2265/3545，并补一句「数字会继续变，别把它当成承诺」
- [x] `教练技能库设计文档.md` 抬头加「口径订正（2026-09-24）」块，
      含新的实测字数与「常驻性价比要重算这 265 字」的提醒
- [x] 后端 `go test ./services/ -run TestSkill` 通过
      （内容抽查断言要求正文里有「四二法则」，本轮没有动到它）
- [x] 后端 `go build ./...` 与 `go vet ./services/` 通过
- [x] 前端 `check:odds` 16 项全绿（含表 4 的 12/12 等式断言）
- [ ] **未做：真机复测**。本轮只改了 265 字文字，**没有动提示词分层、约束条数或触发器**，
      按 §9.2 的口径不需要重跑整轮；但如果你要一个「教练确实不再说『任何听牌』」的
      实证，就得跑一次真实手牌（一次分析约 7-8 分钟）。**要不要跑由你定。**

> §4.4 的目录条目示例、`复盘功能设计文档.md` §3.2/§3.3 的分层图与约束条数
> **不受本次改动影响** —— 本次没动技能篇数、没动触发器，也没动
> `prompt_builder.go` 里的硬性约束。

---

## 3. 功能设计

### 3.1 入口

「我的」页（`src/pages/profile/index.tsx`）菜单区加一个 `Cell`，
放在「历史战绩」和「设置」之间：

```
📜 历史战绩
🎯 赔率快速训练     ← 新增 data-testid="btn-odds-drill"
⚙️ 设置
```

**独立页面**，不是内嵌在「我的」页里：训练要连续答 10 题，内嵌的话滚动位置、
返回手势和底部 Tab 都会打架。走 `Taro.navigateTo({ url: '/pages/odds-drill/index' })`，
与 `settings` → `settings-blind` 的既有写法一致。

### 3.2 页面与流程

```
[训练页]
  ┌ 顶部：本组进度 3/10 ｜ 连击 4 ｜ 正确率 80%
  ├ 题干卡（大字号局面描述）
  │    「翻牌圈，底池 10bb。你下注 2/3 池。
  │      对手是同花听。他需要多少胜率才能跟？」
  ├ 作答区
  │    [ 20% ] [ 25% ] [ 29% ] [ 33% ]     ← 选择题（默认，快）
  │    或 [ ____% ] 数字键盘               ← 填空题（难档）
  ├ 提交后 → 解释卡（§3.5）
  └ 底部 [ 下一题 ]
[本组结束] → 小结：正确率 / 用时 / 错题回顾 / [再来一组]
```

**默认选择题**是刻意的：训练目标是**条件反射**，不是算术能力。
四选一能让人在 3 秒内作答，逼出「记没记住」；填空题留给难档。

### 3.3 五种题型

五种题型一一对应 `odds-table.md` 的四张表，不发明新东西：

| # | 题型 | 对应文档 | 题干示例 | 答案 |
|---|---|---|---|---|
| **T1** | 面对下注报门槛 | 表 1 | 对手下注 1/3 池，我需多少胜率？ | 20% |
| **T2** | 加注后报对手门槛 | 表 2（双向读） | 对手下注 1/2 池，我加注到 2 倍，他需多少胜率？ | 17% |
| **T3** | 听牌换算 | 表 4 | 转牌圈，同花听（9 outs），单街胜率？ | 20% |
| **T4** | 反查最大可跟尺度 | 表 3 | 我有 25% 胜率，最多能跟多大下注？ | 1/2 池 |
| **T5** | 判断题（门槛 × 胜率） | 表 1/4 交汇 | 转牌圈，对手下注 1/2 池，我是同花听。跟注对吗？ | **错**（需 25%，只有 20%） |

T1/T2 是纯算术，T3/T4 是换算，**T5 才是真正的训练目的** ——
文档「三条推论」里的第 ② 条就是 T5 的算术来源，T5 把这条从结论变成练习题。
（该条原文写的是「用**任何**听牌跟注都是错误」，2026-09-24 已收窄为
「9 张 outs 及以下」，理由见 §2.7 与 §2.8。）

### 3.4 判定与容差

- 答案用**精确值**（§2.2 的组合公式），判定容差 **±1.5 个百分点**。
- 容差是必要的：文档自己就把 16.67 写成 17、22.22 写成 22。
  容差太紧会把「答对了」判成错，这是训练器最容易犯的错。
- **四二法则单独一档**：题目会明确问「用四二法则估算」还是「精确值」。
  两者都教，但**永不混判** —— 转牌 9 outs 的四二法则给 18%、精确值 20%，
  问哪个就该答哪个。解释卡把两者并排列出（§3.5）。
- 选择题的干扰项 = 相邻档位的门槛值（如问 1/3 池，干扰项给 1/4 / 1/2 / 1 倍池的门槛），
  这样选错能立刻看出「尺度和门槛的对应关系记串了」。

### 3.5 解释卡（答完必看）

答案本身不是价值，**算式才是**。每题答完后固定展示三层：

```
✅ 需要 29%（精确 28.57%，容差内）

算式：跟注额 6.7 ÷ 跟注后的底池 23.3 = 28.6%
       （池 10 + 我下注 6.7 + 我跟 6.7）

口诀：2/3 池 → 约 29%（1/3→20% 1/2→25% 1→33% 2→40%）

对手实际胜率：同花听 9 outs，双街 35% / 单街 20%
→ 他这个跟注：翻牌圈划算（35% > 29%），转牌圈不划算（20% < 29%）
```

最后一行就是 T5 的雏形 —— **即使是 T1/T2 的算术题，也顺手把结论给出来**，
让用户每次都在「门槛 vs 实际胜率」的对照里泡着。

### 3.6 难度分级

| 档 | 下注尺度 | outs | 作答 | 备注 |
|---|---|---|---|---|
| 1 入门 | 只有 1/3、1/2、1 倍池 | 只有 9（同花听） | 选择题 | 先把 20/25/33 三个锚点焊死 |
| 2 常规 | 表 1 的全部 11 档 | 表 4 的全部 6 种听牌 | 选择题 | —— |
| 3 进阶 | 任意尺度（如 0.7 倍池）+ 加注倍数任意 | 任意 outs | 填空题 | 考公式本身，而非查表 |
| 4 混合 | 随机 | 随机 | 判断 + 填空 | T5 为主 |

文档 §十 的锚点（**1/3 池 → 20%，1/2 池 → 25%，1 倍池 → 33%，2 倍池 → 40%**）
就是档 1 的题库骨干。

---

## 4. 技术设计

### 4.1 文件布局

```
src/utils/potOdds.ts           # 纯函数引擎（无 React / Taro 依赖）
src/utils/oddsDrill.ts         # 出题器与判分（纯函数）
src/utils/oddsDrillProgress.ts # 进度逻辑与防御性解析（纯函数）
src/utils/oddsDrillStorage.ts  # 本地进度读写（唯一碰 Taro 的地方，薄封装）

src/pages/odds-drill/
  ├ index.tsx        # 页面（仅 UI）
  ├ index.config.ts  # navigationBarTitleText: '赔率快速训练'
  ├ index.less
  └ useOddsDrill.ts  # 页面状态与流程 Hook

tests/odds-drill.spec.ts       # e2e
tools/odds-check/              # 常驻对拍脚本
```

> 进度逻辑与 Taro 读写**故意分成两个文件**（M4 时改的，原设计是一个）：
> 只要 import 了 Taro，防御性解析就进不了 node，而「读到脏数据不能崩」
> 恰好最该被常驻对拍覆盖。理由与 `cards.ts` 和 `CardPicker` 分家是同一条。
>
> 页面目录里没有 `types.ts`：页面自己的类型（Props、状态）都在
> `useOddsDrill.ts` 里，单开一个文件只会放一个不需要的 re-export。

src/pages/profile/index.tsx    # 加入口 Cell（唯一改动的既有文件）
src/app.config.ts              # 注册 'pages/odds-drill/index'

tools/odds-check/              # §5 M1 的常驻对拍脚本
```

### 4.2 纯函数引擎 `src/utils/potOdds.ts`

```ts
/** 一次跟注决策：池、对手下注额、可选择“加注到多少”（都以 bb 为单位） */
export interface CallSpot {
  pot: number;          // 对手下注前的底池
  bet: number;          // 对手的下注额
  raiseTo?: number;     // 我加注后的总额（含）；不传 = 我不加注，直接跟
}

/** 唯一入口：跟注额 ÷ 跟注后的底池 */
export function requiredEquity(spot: CallSpot): number;

/** 赔率 x:1 ↔ 胜率 */
export function equityToPotOdds(eq: number): number;   // (1-eq)/eq
export function potOddsToEquity(odds: number): number; // 1/(odds+1)

/** outs → 胜率。exact = 组合数精确值，rule = 四二法则口诀值 */
export function outsToEquity(outs: number, street: 'flop' | 'turn'):
  { exact: number; rule: number };

/** 反查：有 eq 胜率时最多能跟几倍池；eq ≥ 50% 返回 null（任意尺度） */
export function maxCallFraction(eq: number): number | null;

/** 展示用：0.3333 → "1/3 池"；0.7 → "0.7 倍池" */
export function fractionLabel(fraction: number): string;
```

`requiredEquity` 的签名刻意设计成**一个接口覆盖全部题型**：
T1 传 `{pot, bet}`，T2 传 `{pot: potBeforeOpponentBet, bet, raiseTo}` 并把结果读作「对手的」，
T4 是它的反函数。**四种题型只有一个真相来源**，避免出现「表 1 和表 2 各自实现一遍、
某天改了一处」的漂移。

### 4.3 出题器 `src/utils/oddsDrill.ts`

```ts
export type DrillKind =
  | 'face-bet'        // T1
  | 'after-raise'     // T2
  | 'outs-to-equity'  // T3
  | 'max-call'        // T4
  | 'call-or-fold';   // T5

export interface Drill {
  kind: DrillKind;
  difficulty: 1 | 2 | 3 | 4;
  /** 渲染好的题干文本 */
  prompt: string;
  /** 选择题选项（含正确答案），填空题为空 */
  choices: number[];
  /** 判定基准 + 容差（单位：胜率的小数，非百分数） */
  expected: number;
  tolerance: number;
  /** 解释卡的算式分解与结论，逐行 */
  steps: string[];
  /** T5 专用 */
  verdict?: 'call' | 'fold';
}

export function generateDrill(
  kind: DrillKind,
  difficulty: 1 | 2 | 3 | 4,
  rng: () => number,
): Drill;

export function gradeAnswer(drill: Drill, input: number):
  { correct: boolean; delta: number };
```

`rng` 从参数传入而不是内部 `Math.random()`：这样**同一 seed 能复现同一套题**，
e2e 测试才能断言具体数字（这是 `tools/open-size-ev/rng.ts` 里已有的做法）。

### 4.4 本地存储（`oddsDrillProgress.ts` + `oddsDrillStorage.ts`）

沿用 `review-create` 里 `Taro.getStorageSync(DRAFT_STORAGE_KEY)` 的写法：

```ts
// 实际实现（比初稿多了 elapsedMs、少了用不上的 streak）
export interface DrillAttempt {
  drill: Drill;      // 存整个 Drill —— 错题要能原样重练
  input: number;
  correct: boolean;
  elapsedMs: number; // 平均用时靠它
}

export interface DrillProgress {
  total: number;     // 累计**作答次数**，重练也计入
  correct: number;
  bestStreak: number;
  byKind: Record<DrillKind, { total: number; correct: number }>;
  wrong: DrillAttempt[]; // 还没答对的，最新在前，上限 20 条
}
```

- **存本地，不落库**（本方案不改后端）。换设备不同步，这是明确接受的代价。
- key 带版本号 `_v1`：将来改结构时不会读到旧结构的脏数据。
- 错题只留最近 20 条，防止存储无限增长（小程序 storage 单项有上限）。
- 初稿里的 `streak`（当前连击）去掉了：它是**单次会话内**的状态，
  存下来的「上次退出时的连击」没有意义；`bestStreak` 才有。
- 每次作答都落一次盘，中途退出不丢；读写都 try/catch ——
  训练本身是纯本地的，不依赖任何一次读写成功。

### 4.5 为什么放 `utils/` 而不是组件目录

和 `src/utils/cards.ts` 开头的理由完全一样：这些是纯函数，
录入页、详情页、将来的计算器都要用；放进组件文件里会把任何想复用它的人
拖进 React 和 Taro 的依赖里，**也就没法脱离浏览器单独跑测试**——
而本方案最关键的验收（§5 M1 的逐格对拍）恰恰需要脱离浏览器跑。

---

## 5. 里程碑拆解

按「每档一交付 + 验收清单」推进，**不在同一档里顺手往下做**。

### M1 · 引擎 + 常驻对拍脚本（零 UI）—— ✅ 已完成（2026-09-24）

**交付**：`src/utils/potOdds.ts`、`src/utils/oddsDrill.ts`、`tools/odds-check/`
（npm script `check:odds`）。

对拍脚本**做成常驻工具而不是临时脚本**（放 `tools/` 下、照 `tools/open-size-ev` 的样子
配自己的 tsconfig + npm script），因为它守的是**漂移**：哪天 `odds-table.md` 改了、
或引擎被人重构了，这个脚本要变红。临时脚本验完就删，守不住这件事。

**范围调整（记录在案）**：`oddsDrill.ts` 在 M1 只出 **T1/T2 两种题型**（档 1~3），
T3/T4/T5 与档 4 显式抛错指明"属于 M3"。理由：M1 的验收对象是**公式**，
题型是公式的消费者；把题型和它的 UI 放在同一档里做，M2 才有完整的交付物可验收。
引擎（`potOdds.ts`）不受此限，**四张表全部实现并全部被对拍覆盖**。

**验收清单**（`npm run check:odds` 10 项全绿）：

- [x] `npm run check:odds` 逐格打印表 1（11 格）、表 2（16 格）、表 3（8 格）、表 4（12 格）
- [x] 表 1 / 表 2 / 表 3 / 表 4单街：**精确复现文档**（不是"约等于"）
- [x] 表 4 双街的卡顺按 §2.4 的拍板结果处理，脚本里留注释说明这是已知例外
- [x] 容差覆盖：文档每一格的取整值都落在精确值的 ±1.5pp 内（§2.6）
- [x] 边界断言：`eq = 50%` 与 `60%` 时 `maxCallFraction` 返回 `null`；`49%` 给出有限大尺度
- [x] 对称性断言：99 个整数百分比的 `赔率 ↔ 胜率` 往返一致
- [x] 单调性断言：0.05→100 倍池门槛严格递增，且**永远 < 50%**（100 倍池才 49.75%）
- [x] 同 seed 生成 100 题，两次结果完全一致，且选项里含正确答案

**过程中抓到的真 bug（值得留一笔）**：上面的「选项里含正确答案」这条断言
第一次跑就红了 —— T2 的干扰项原先按**下注尺度**的门槛生成，而 T2 的答案是
**加注后**的门槛，两者不是一个数，于是 100 题里有 28 题的正确答案不在选项中。
修法是让干扰项走**与本题同口径**的算法（`buildChoices` 收一个 `equityOfFraction` 回调）。
如果没有这条断言，这个 bug 会一路带到 M2 的界面上才被发现。

### M2 · 页面骨架 + T1/T2 + 入口 —— ✅ 已完成（2026-09-24）

**交付**：`src/pages/odds-drill/`（`index.tsx` / `index.config.ts` / `index.less` /
`useOddsDrill.ts`）、`app.config.ts` 注册、profile 入口 Cell、`tests/odds-drill.spec.ts`。

**范围调整（记录在案）**：M2 只开放**前两档难度**（入门/常规）。档 3 要填空作答、
档 4 是混合判断题，与 T3~T5 一起留在 M3 —— 否则这一档要同时交付「选择作答」与
「填空作答」两套交互。小结页在 M2 只给正确率/答对数/最高连击，
平均用时与错题回顾按计划在 M4 补（那才是它该在的地方：错题要能重练）。

**验收清单**：

- [x] 「我的」页出现「🎯 赔率快速训练」，点击进入训练页（真 token 下实点验证通过）
- [x] 能连答 10 题，顶部进度 / 连击 / 正确率实时更新
- [x] 每题答完展示解释卡（算式 + 口诀 + 锚点）
- [x] 答错时标红自己的选择、标绿正确项，并说明差了几个百分点
- [x] 难度可切换，切换即重开一组（进度与统计清零）
- [x] e2e 4 条全绿（`4 passed`）

**e2e 的断言方式值得记一笔**：它不查种子表，而是**从页面上读回题干 → 用同一个引擎
`src/utils/potOdds.ts` 算出答案 → 去点对应的选项**。所以这一条同时守住三件事：
题干渲染、选项生成、判分口径 —— 三者任意一个偏了都会红。四条覆盖：
10 题全对（小结 100%）、同 seed 可复现、答错时的染色与差值提示、切难度重开一组。

**页面需要重启 dev server 才会被认到**：Taro 增删页面（`app.config.ts`）在
`--watch` 下不生效，必须重启。本次重启了 3000 上的 dev server（按 PID 停、原样拉起）。

**M2 暴露的一个环境问题**：仓库的 `playwright.config.ts` 目标是 chromium，
但本机没有下载（`~/Library/Caches/ms-playwright` 是空的），只有系统 Edge。
本次是用一份**临时 config**（`channel: 'msedge'`）跑的，没有改仓库配置。
要让 `npm run test:e2e` 在本地直接可用，二选一：`npx playwright install chromium`
（约 120MB），或把 config 的 `channel` 改成可配置。**待你定**，见 §8 第 6 条。

### M3 · T3 / T4 / T5 + 难度分级 —— ✅ 已完成（2026-09-24）

**交付**：`oddsDrill.ts` 补齐 T3/T4/T5 与四档题库、`useOddsDrill.ts` 加填空作答、
页面加判断题按钮与填空输入、`check:odds` 扩到 14 项、e2e 扩到 6 条。

**验收清单**：

- [x] T3 四二法则档与精确值档**分开问、分开判**（题目明确写「精确值」或「四二法则估算」，
      答案取对应的那个；四二法则在 15 outs 时高估 6pp，超出容差，所以两档是真分得开的）
- [x] T5 判断题按**数学**判对错，解释卡按 §2.7 的口径说明与文档推论② 的关系
      （**原清单写的「与推论② 一致」是错的，见 §2.7** —— 推论② 本身过度概括）
- [x] 四档难度可切换，档 1 的题库只含 1/3、1/2、1 倍池 + 9 outs（同花听）
- [x] 档 3/4 的填空题接受 `20`、`20%`、`0.2` 三种输入形式（另有 5 种垃圾输入被拒）
- [x] `npm run check:odds` 14 项全绿；e2e 6 条全绿

**改掉的一条原设计**：原清单要求「T4 的『任意尺度』(eq ≥ 50%) 有专门的答案文案」。
实现时改了：T4 的目标胜率取自**文档表 3 自己的行**（17%~45%），全部小于 50%，
所以这个情况由构造就不出现。

理由：**胜率过半就不存在「最大可跟尺度」** —— 跟多少都对，这道题没有可答的答案。
文档表 3 也没列 ≥50% 的行，正是因为如此。「任意尺度」这个概念改由 **T5 承接**：
15 outs 双街 54% 面对任何下注都该跟，解释卡会说清这一层。
`maxCallFraction` 返回 `null` 的分支仍留着做防御（万一将来加了 ≥50% 的目标），
文案是「无上限」而不是「null 倍池」。

**过程中脚本抓到的三个真问题**（都不是测试的问题，是实现的）：
1. 档 1 的反查题用了完整命名档位求解，答案可能是 **1/4 池** —— 而档 1 的设计是
   「只练 1/3、1/2、1 倍池」。修法：求解读本档的尺度池，并把在该池里无解的目标
   （如 17%，因为 1/3 池门槛就有 20%）先从目标集里剔掉。
2. 同一处：答案是最小的 1/4 池时前面没有更小的邻居，**选项凑不满 4 个**。修法：
   干扰项的候选集加一份完整命名档位做兜底。
3. 我把「门槛必然 < 50%」的断言错用到了 T3 上 —— T3 问的是**成牌概率**，
   15 outs 双街 54% 是合法答案。修法：该断言只对 T1/T2 生效。



### M4 · 进度存储 + 小结页 —— ✅ 已完成（2026-09-24）

**交付**：`src/utils/oddsDrillProgress.ts`（纯逻辑）、`src/utils/oddsDrillStorage.ts`
（Taro 读写）、`odds-drill/` 的小结页（本组 + 错题 + 累计）、`check:odds` 扩到 16 项、
e2e 扩到 9 条。

**范围调整（记录在案）**：原 §4.1 只规划了一个 `oddsDrillStorage.ts`，
实现时拆成两个文件 —— 逻辑与防御性解析放 `oddsDrillProgress.ts`（纯），
Taro 读写放 `oddsDrillStorage.ts`（薄封装）。

理由：**只要 import 了 Taro，防御性解析就进不了 node**，而「读到脏数据不能崩」
恰好是最该被常驻对拍覆盖的一条。拆开之后这一条由 `check:odds` 守着，
不需要靠浏览器去试（14 种脏输入常驻跑）。这与 `cards.ts` 与 `CardPicker` 分家的
理由是同一条。

**验收清单**：

- [x] 答满一组后小结展示：正确率、答对数、最高连击、**平均用时**、**错题列表**
- [x] 杀掉小程序重进，累计进度与历史最高连击还在（e2e 里答 3 题 → reload → 累计 13 次作答）
- [x] 错题回顾能直接重练**同一道题**（存的是整个 `Drill`，题目、选项、答案一模一样），
      答对后自动从错题列表出列
- [x] `odds_drill_progress_v1` 读到脏数据不崩：14 种脏输入全部解析成可用进度，
      认不出的错题条目被丢弃、`correct` 超过 `total` 会被夹回去

**存整个 Drill 而不是只存摘要**：原 §4.4 的错题结构写的是
`{ kind, prompt, expected, input }`。实现时改成存完整的 `Drill` —— 因为要「重练同一题」，
只有摘要就得靠种子+下标去重新生成，脆弱且容易对不上。代价是每条错题几百字节，
20 条上限下可以忽略。

**修掉的一个真 bug**：底部按钮的文案原先用**本组**错题数，而 `retryWrong()`
重练的是**累计**错题 —— 两者不等时（以前还错着别的题）文案会骗人。
按钮改成「重练错题（共 N 道）」，N 用累计数。

**一处设计选择**：累计口径是**作答次数**而不是题目数，所以重练也计入
`total`/`correct`。理由：每次作答都是一次练习，按题去重反而会让「反复错的题」
在正确率里被稀释掉。

---

## 6. 非目标（刻意不做）

1. **牌面识别 / 自动数 outs**。用户选底牌和公共牌、系统自己判断听牌类型并数 outs ——
   需要新写一套 outs 检测（同花听、两头顺、卡顺、组合听牌的叠加与去重），
   属于独立一块工作量。**v1 的题目直接给「同花听 9 outs」这类描述**，
   因为本方案练的是**赔率算术**，不是读牌。
2. **精确 equity 枚举 / 蒙特卡洛**。需要把 `tools/open-size-ev/evaluator.ts`
   从 Node CLI 工具搬进 `src/utils` 共享（它现在是 `tsc` 单独编译、不在 app 的
   `include` 里），是一次实打实的重构。
3. **后端落库**。不做训练记录表、不做多端同步。
4. **排行榜 / 与复盘画像联动**。等有了后端数据再说。
5. **隐含赔率与抽水建模**。文档明确「未计抽水」，隐含赔率更是无法纯算术得出
   （要看对手的支付意愿）。**建模这两者会让训练器与教练口径不一致**，不做。
6. **自由输入的计算器页**。每题答完的解释卡已经等于半个计算器（算式、池、
   跟注额都列出来了）。真要自由输入，是 M5 的事，先不做。

---

## 7. 风险与取舍

### 7.1 数值漂移（最大风险）

`odds-table.md` 是**后端技能层**文件，改了它，训练器不会自动跟着改。
`教练文档漂移回查点` 那条经验说明改技能层要连带回查三处文档 —— 现在要加上**第四处：本训练器**。

**缓解**：§5 M1 的 `tools/odds-check` 常驻脚本，把文档的锚点值写成断言。
文档一改、或者引擎一改，脚本就红。**这是本方案里最值钱的一个设计**。

### 7.2 让用户「算得对但打错」

文档自己就警告过：表 2 那些数「不能只凭这张表就多跟」（对手的加注范围也更窄，
两个效应方向相反）。训练器如果只教算术，会培养出**不看范围、只背门槛**的习惯 ——
这比不会算更糟。

**缓解**：解释卡每次把「门槛」和「对手的实际范围强度」并列提示；
档 4 的 T5 判断题里混入「门槛够但范围不对」的局面，并明确说明这是算术之外的判断。

### 7.3 免责声明的口径

`AnalysisPanel` 里有一句「AI 建议仅供参考。此处不提供精确 EV 与胜率数字」，
而复盘设计文档 §3.3 第 7 条明令**提示词层不许编造精确 EV / 胜率**。

训练器给的是**纯数学事实**（组合数算出来的），不是 AI 建议，两者不冲突。
但 UI 上要说清楚这个区别，避免用户以为「训练器给的 35% 就是 AI 认可的胜率」：

> 训练器给的是纯概率算术（无抽水、无隐含赔率、不读对手范围）。
> 真实决策请结合对手范围判断。

### 7.4 答对不等于学会

选择题有四选一，蒙对率 25%。**缓解**：正确率与「容差内命中率」分开记；
档 3 强制填空，用不用选择题来决定是不是真会算。

---

## 8. 拍板记录（2026-09-24，全部按推荐执行）

| # | 事项 | 结论 |
|---|---|---|
| 1 | 卡顺 4 outs 双街的取值（§2.4） | **判分用精确值 16.47% + ±1.5pp 容差**，解释卡点名边界。16% 与 17% 两种答法都判对。（**2026-09-24 续：文档已订正为 16%，见 §2.8**） |
| 2 | T5 判断题做不做 | **做**。只做 T1~T4 的话就退化成算术练习，达不到训练目的 |
| 3 | 独立页还是内嵌「我的」页 | **独立页** `pages/odds-drill/`，走 `navigateTo` |
| 4 | 是否给 `odds-table.md` 表 2 加「对调座位同样适用」的注释 | **先不动**（2026-09-24 续：三处口径问题已订正，见 §2.8；表 2 那句注释仍未加） |
| 5 | 每组题量与默认难度 | **10 题一组，默认档 2** |
| 6 | 本地怎么跑 e2e（M2 完成后追加） | **待定**：本机没装 playwright 的 chromium，只有系统 Edge。可选 `npx playwright install chromium`（约 120MB）/ 把 config 的 channel 改成可配置 / 维持现状用临时 config |

**设计后追加确认的一处**（§2.5）：表 3 反查走**文档取整口径**才能 8/8，
引擎里 `largestCallable` 只用于判分、`maxCallFraction` 用于解释卡里的精确边界。

**下一档（M2）的验收预告**：「我的」页出现入口 → 进入训练页 → 连答 10 题 T1/T2 →
每题展示解释卡 → 小结页；另加一条 playwright e2e 走通同一条路径。

---

## 附：对拍脚本的关键片段

M1 的自测核心就是这个（§2.2 的数就是这么来的）：

```ts
const C2 = (n: number) => (n * (n - 1)) / 2;

/** 唯一公式：跟注额 ÷ 跟注后的底池 */
const requiredEquity = (pot: number, bet: number) => bet / (pot + 2 * bet);

/** 我先下注 b、对手加注到 X·b：我需补 b(X−1)，跟注后池 = 1 + 2Xb */
const afterRaise = (b: number, X: number) => (b * (X - 1)) / (1 + 2 * X * b);

/** 双街（翻牌→河牌，未见 47 张）精确胜率 */
const twoStreet = (outs: number) => 1 - C2(47 - outs) / C2(47);

/** 单街（转牌→河牌，未见 46 张） */
const oneStreet = (outs: number) => outs / 46;

// 表 1：b = 1/4,1/3,1/2,2/3,3/4,1,1.5,2,3,5,10
//       → 17,20,25,29,30,33,38,40,43,45,48  全部四舍五入一致
// 表 2：b ∈ {1/3,1/2,2/3,1}，X ∈ {2,2.5,3,4} → 16/16 一致
// 表 4：outs ∈ {4,6,8,9,12,15} → 单街 6/6 一致，双街仅卡顺(4) 差 0.53pp
```
